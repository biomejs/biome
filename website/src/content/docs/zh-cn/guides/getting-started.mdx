---
title: 入门指南
description: 学习如何使用 Biome 设置新项目。
---

import PackageManagerBiomeCommand from "@src/components/PackageManagerBiomeCommand.astro";
import PackageManagerCommand from "@src/components/PackageManagerCommand.astro";

## 系统要求

- Windows（包括 WSL）、macOS 或 Linux
- x86_64 或 ARM64
- Node.js v14.18 或更高版本（如果使用独立可执行文件则不适用）

## 安装

下载 Biome 的最快方式是使用 `npm` 或你喜欢的包管理器。如果你想在不安装 Node.js 的情况下使用 Biome，CLI 也提供了[独立可执行文件](/guides/manual-installation)。

要安装 Biome，请在包含 `package.json` 文件的目录中运行以下命令。

<PackageManagerCommand
  npm="install --save-dev --save-exact @biomejs/biome"
  pnpm="add --save-dev --save-exact @biomejs/biome"
  yarn="add --dev --exact @biomejs/biome"
  bun="add --dev --exact @biomejs/biome"
/>

:::note
也可以全局安装 Biome 而不是本地安装。
但是，我们不推荐这样做。
:::

我们**强烈建议**在安装 Biome 时不要使用范围操作符。
更多信息请查看[版本页面](/internals/versioning/)。

## 配置

我们建议为每个项目创建一个 `biome.json` 配置文件。
这样可以避免每次运行命令时都要重复 CLI 选项，并确保 Biome 在你的编辑器中应用相同的配置。如果你对 Biome 的默认设置感到满意，你不必创建配置。

要创建配置，请在项目的根文件夹中运行 `init` 命令：

<PackageManagerBiomeCommand command="init" />

运行 `init` 命令后，你的目录中现在会有一个新的 `biome.json` 文件：

```json title="biome.json"
{
  "$schema": "https://biomejs.dev/schemas/1.4.0/schema.json",
  "organizeImports": {
    "enabled": false
  },
  "linter": {
    "enabled": true,
    "rules": {
      "recommended": true
    }
  }
}
```

`linter.enabled: true` 启用了 linter，`rules.recommended: true` 启用了[推荐规则](/linter/rules/)。

因为配置没有明确地使用 `formatter.enabled: false` [禁用](/reference/configuration/#formatterenabled) 格式化，所以格式化是启用的。

## 使用

Biome CLI 提供了许多命令和选项，所以你可以只使用你需要的部分。

你可以使用 [`format`](/reference/cli#biome-format) 命令和 `--write` 选项格式化文件和目录：

<PackageManagerBiomeCommand command="format <files> --write" />

你可以使用 [`lint`](/reference/cli#biome-lint) 命令和 `--apply` 选项对文件和目录进行 lint 并应用[安全修复](/linter#safe-fixes)：

<PackageManagerBiomeCommand command="lint <files>" />

你可以通过使用 [`check`](/reference/cli#biome-check) 命令应用**两者**：

<PackageManagerBiomeCommand command="check --apply <files>" />

`check` 命令是用于一次运行多个工具的命令。目前，它可以：

- 格式化文件
- lint 文件
- 组织导入

## 安装编辑器插件

我们建议安装编辑器插件以充分利用 Biome。查看[编辑器页面](/guides/integrate-in-editor)了解哪些编辑器支持 Biome。

## CI 设置

如果你正在使用 Node.js，那么在 CI 中运行 Biome 的推荐方式是使用[你最喜欢的包管理器](/guides/getting-started#installation)。
这确保你的 CI 流水线使用的 Biome 版本与你在编辑器中或运行本地 CLI 命令时使用的版本相同。

## 下一步

成功！你现在已经准备好使用 Biome 了。🥳

- 了解更多关于如何使用和配置[格式化器](/formatter)的信息
- 了解更多关于如何使用和配置[linter](/linter)的信息
- 熟悉[CLI 选项](/reference/cli)
- 熟悉[配置选项](/reference/configuration)
- 加入我们的[Discord 社区](https://discord.gg/BypW39g6Yc)

