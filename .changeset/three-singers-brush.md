---
"@biomejs/biome": minor
---

Added `biome inspect plugins` to list resolved rules by plugin and importing preset without loading or executing plugin code. Include globs show which files each import targets, `--path` selects a target file, and `--json` provides the resolved inventory and errors. Fixed Markdown linter overrides and their configuration inspection.
