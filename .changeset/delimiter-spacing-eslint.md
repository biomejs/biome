---
"@biomejs/biome": patch
---

Added ESLint migration mappings for the `delimiterSpacing` formatter option. The following rules now point to `delimiterSpacing`: `array-bracket-spacing`, `computed-property-spacing`, `space-in-brackets`, `space-in-parens`, `template-curly-spacing`, and `jsx-curly-spacing` (across `eslint`, `@stylistic`, `react`, and `vue` plugins).
