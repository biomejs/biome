---
"@biomejs/biome": minor
---

Added support for `.svg` files. Biome now recognizes `.svg` files as HTML, enabling parsing, formatting, and linting for SVG files. For example, the `noSvgWithoutTitle` accessibility rule can now be applied directly to `.svg` files.
