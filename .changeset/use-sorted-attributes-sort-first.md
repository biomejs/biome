---
"@biomejs/biome": minor
---

Added the `sortFirst` option to the [useSortedAttributes](https://biomejs.dev/assist/actions/use-sorted-attributes/) assist (JSX and HTML). It takes a list of attribute names that are sorted before all other attributes, in the order given, which is useful to keep attributes such as `key` first.
