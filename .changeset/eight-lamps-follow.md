---
"@biomejs/biome": minor
---

Added `resolvePath` for object-syntax plugin entries. Plugin paths can now be resolved relative to the consuming workspace or the configuration file that declared the plugin.
