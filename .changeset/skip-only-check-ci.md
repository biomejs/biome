---
"@biomejs/biome": minor
---

Added `--only` and `--skip` options to `biome check` and `biome ci`. Biome now lets you run or exclude specific rules, groups, or domains when running these commands.
