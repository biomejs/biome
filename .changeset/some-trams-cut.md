---
"@biomejs/biome": patch
---

Added HTML support for the [`noRedundantRoles`](https://biomejs.dev/linter/rules/no-redundant-roles/) lint rule. Biome now detects redundant explicit `role` attributes on HTML elements (e.g., `<button role="button">`).
