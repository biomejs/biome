---
"@biomejs/biome": patch
---

Added `ignoreMixedLogicalExpressions`, `ignorePrimitives`, `ignoreBooleanCoercion`, `ignoreIfStatements` options and if-statement detection to `useNullishCoalescing`, reaching feature parity with ESLint's `prefer-nullish-coalescing`.
