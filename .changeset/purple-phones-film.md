---
"@biomejs/biome": patch
---

Fixed [#5242](https://github.com/biomejs/biome/issues/5242): HTML `style` attribute values are now formatted as CSS declarations, and `class` attribute values are now formatted as space-separated class names, removing extra whitespace between classes.
