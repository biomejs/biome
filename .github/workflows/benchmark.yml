name: Benchmarks

on:
  workflow_dispatch:
  pull_request:
    types: [ opened, synchronize ]
    branches:
      - main
    paths:
      - 'Cargo.lock'
      - 'crates/**_parser/**/*.rs'
      - 'crates/**_formatter/**/*.rs'
      - 'crates/**_analyze/**/*.rs'
  push:
    branches:
      - main
    paths:
      - 'Cargo.lock'
      - 'crates/**_parser/**/*.rs'
      - 'crates/**_formatter/**/*.rs'
      - 'crates/**_analyze/**/*.rs'

env:
  RUST_LOG: info

jobs:
  bench:
    permissions:
      pull-requests: write
    name: Bench
    runs-on: ubuntu-latest

    steps:

      - name: Checkout PR Branch
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b # v4
        with:
          ref: ${{ steps.sha.outputs.result }}

      - name: Install toolchain
        uses: moonrepo/setup-rust@b8edcc56aab474d90c7cf0bb8beeaf8334c15e9f # v1
        with:
          channel: stable
          cache-target: release
          bins: cargo-codspeed
          cache-base: main

      - name: Compile
        run: cargo codspeed build --features codspeed -p xtask_bench

      - name: Run the benchmarks
        uses: CodSpeedHQ/action@ac302de34d7033a41328af8feef9994251c7bd1e # v2
        timeout-minutes: 30
        with:
          run: cargo codspeed run
          token: ${{ secrets.CODSPEED_TOKEN }}
