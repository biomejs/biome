This is `inline code` in a sentence.

`code at start` of a line.

End of a line `code at end`.

`single word`

``double backtick``

``` triple backtick ```

`code with spaces   inside`

`  leading spaces`

`trailing spaces  `

`  both sides  `

`` `backtick inside` ``

`` ` ``

`code with *asterisks* inside`

`code with **double asterisks** inside`

`code with [link](url) inside`

`a` and `b` and `c`

Some `inline` code `mixed` with text `throughout` the paragraph.

This is a long paragraph with `some inline code` embedded in it to test that it does not affect line width calculations incorrectly at all.

`fn main() { println!("hello"); }`

`<div>html tags</div>`

``foo ` bar``

`` foo ` bar ``

`   `

`     lots     of     spaces     `

`	tab	separated	`

`  multiple   spaces   between   words  `

`a  b  c`

`  a  `

` x`

`x `

`   leading only`

`trailing only   `

`one two  three   four    five`

`` has  spaces  and ` backtick ``
