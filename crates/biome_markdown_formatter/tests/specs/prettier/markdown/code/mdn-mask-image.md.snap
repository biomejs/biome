---
source: crates/biome_formatter_test/src/snapshot_builder.rs
info: markdown/code/mdn-mask-image.md
---
# Input

```md

```css

/* Multiple values */
mask-image: image(url(mask.png), skyblue),                          linear-gradient(rgb(0 0 0 / 100%), transparent);
```

```


# Prettier differences

```diff
--- Prettier
+++ Biome
@@ -1,3 +1,4 @@
+
 ```css
 
 /* Multiple values */
```

# Output

```md

```css

/* Multiple values */
mask-image: image(url(mask.png), skyblue),                          linear-gradient(rgb(0 0 0 / 100%), transparent);
```
```

# Lines exceeding max width of 80 characters
```
    5: mask-image: image(url(mask.png), skyblue),                          linear-gradient(rgb(0 0 0 / 100%), transparent);
```
