---
source: crates/biome_formatter_test/src/snapshot_builder.rs
assertion_line: 211
info: js/binary-expressions/parentheses/bitwise.js
---
# Input

```js
// https://github.com/prettier/prettier/issues/18145
1 << bit % 8;
1 >> bit - 8;
1 | bit % 8;
1 & bit % 8;
1 ^ bit % 8;
1 >>> bit % 8;
(a + b) % c;
a % (b + c);
1 << bit * 8;
1 >> bit / 8;
1 << bit + 8;
1 >> bit - 8;

```


# Prettier differences

```diff
--- Prettier
+++ Biome
@@ -1,10 +1,10 @@
 // https://github.com/prettier/prettier/issues/18145
-1 << bit % 8;
+1 << (bit % 8);
 1 >> (bit - 8);
-1 | bit % 8;
-1 & bit % 8;
-1 ^ bit % 8;
-1 >>> bit % 8;
+1 | (bit % 8);
+1 & (bit % 8);
+1 ^ (bit % 8);
+1 >>> (bit % 8);
 (a + b) % c;
 a % (b + c);
 1 << (bit * 8);
```

# Output

```js
// https://github.com/prettier/prettier/issues/18145
1 << (bit % 8);
1 >> (bit - 8);
1 | (bit % 8);
1 & (bit % 8);
1 ^ (bit % 8);
1 >>> (bit % 8);
(a + b) % c;
a % (b + c);
1 << (bit * 8);
1 >> (bit / 8);
1 << (bit + 8);
1 >> (bit - 8);
```
