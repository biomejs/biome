---
source: crates/biome_formatter_test/src/snapshot_builder.rs
assertion_line: 211
info: js/reserved-word/let.js
---
# Input

```js
foo.let;
let.foo;
new let();
({ let: "foo" });
(let, "foo");
void let;
var let = "foo";

```


# Prettier differences

```diff
--- Prettier
+++ Biome
@@ -2,6 +2,6 @@
 let.foo;
 new let();
 ({ let: "foo" });
-(let, "foo");
+let, "foo";
 void let;
 var let = "foo";
```

# Output

```js
foo.let;
let.foo;
new let();
({ let: "foo" });
let, "foo";
void let;
var let = "foo";
```

# Errors
```
let.js:2:1 parse ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  × Illegal use of reserved keyword `let` as an identifier in strict mode
  
    1 │ foo.let;
  > 2 │ let.foo;
      │ ^^^
    3 │ new let();
    4 │ ({ let: "foo" });
  
let.js:3:5 parse ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  × Illegal use of reserved keyword `let` as an identifier in strict mode
  
    1 │ foo.let;
    2 │ let.foo;
  > 3 │ new let();
      │     ^^^
    4 │ ({ let: "foo" });
    5 │ (let, "foo");
  
let.js:5:2 parse ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  × Illegal use of reserved keyword `let` as an identifier in strict mode
  
    3 │ new let();
    4 │ ({ let: "foo" });
  > 5 │ (let, "foo");
      │  ^^^
    6 │ void let;
    7 │ var let = "foo";
  
let.js:6:6 parse ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  × Illegal use of reserved keyword `let` as an identifier in strict mode
  
    4 │ ({ let: "foo" });
    5 │ (let, "foo");
  > 6 │ void let;
      │      ^^^
    7 │ var let = "foo";
    8 │ 
  
let.js:7:5 parse ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  × Illegal use of reserved keyword `let` as an identifier in strict mode
  
    5 │ (let, "foo");
    6 │ void let;
  > 7 │ var let = "foo";
      │     ^^^
    8 │ 
  

```
