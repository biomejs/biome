switch(5){default: // comment5
// comment5a
foo();bar();//comment5b
break;// comment5c
}

switch(x) {
  default:
    a(); // asd
    break;
}

switch(x) {
  default:
    // a
    a(); // ab
    break;
}
