union SearchResult = Photo |

union SearchResult
	| Photo
	| Person

union SearchResult =
	|
	| Person

union SearchResult =
	| Photo
	|

union SearchResult @

union SearchResult |

union SearchResult =

union SearchResult = |

union SearchResult @ =
