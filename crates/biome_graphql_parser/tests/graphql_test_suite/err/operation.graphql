query likeStory }

query likeStory

# malformed variables
query ($storyId: ID! {
	likeStory(storyId: $storyId)
}

query $storyId: ID! {
	likeStory(storyId: $storyId)
}

query $storyId: ID!) {
	likeStory(storyId: $storyId)
}

query ($: ) {
	likeStory(storyId: $)
}

