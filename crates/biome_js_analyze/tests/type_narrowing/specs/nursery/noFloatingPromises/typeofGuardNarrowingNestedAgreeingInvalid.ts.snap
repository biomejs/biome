---
source: crates/biome_js_analyze/tests/spec_tests.rs
expression: typeofGuardNarrowingNestedAgreeingInvalid.ts
---
# Input
```ts
// should generate diagnostics

// The call floats with or without narrowing. Repeating or reversing the same
// `typeof` check must not make narrowing suppress the diagnostic.
function nestedAgreeingGuards(x: number | (() => Promise<void>)) {
	if (typeof x === "function") {
		if (typeof x === "function") {
			x();
		}
	}
}

// The same holds when the inner guard spells the comparison the other way
// around.
function nestedReversedGuards(x: number | (() => Promise<void>)) {
	if (typeof x === "function") {
		if ("function" === typeof x) {
			x();
		}
	}
}

```

# Diagnostics
```
typeofGuardNarrowingNestedAgreeingInvalid.ts:8:4 lint/nursery/noFloatingPromises ━━━━━━━━━━━━━━━━━━━

  i A "floating" Promise was found, meaning it is not properly handled and could lead to ignored errors or unexpected behavior.
  
     6 │ 	if (typeof x === "function") {
     7 │ 		if (typeof x === "function") {
   > 8 │ 			x();
       │ 			^^^^
     9 │ 		}
    10 │ 	}
  
  i This happens when a Promise is not awaited, lacks a `.catch` or `.then` rejection handler, or is not explicitly ignored using the `void` operator.
  
  i This rule belongs to the nursery group, which means it is not yet stable and may change in the future. Visit https://biomejs.dev/linter/#nursery for more information.
  

```

```
typeofGuardNarrowingNestedAgreeingInvalid.ts:18:4 lint/nursery/noFloatingPromises ━━━━━━━━━━━━━━━━━━

  i A "floating" Promise was found, meaning it is not properly handled and could lead to ignored errors or unexpected behavior.
  
    16 │ 	if (typeof x === "function") {
    17 │ 		if ("function" === typeof x) {
  > 18 │ 			x();
       │ 			^^^^
    19 │ 		}
    20 │ 	}
  
  i This happens when a Promise is not awaited, lacks a `.catch` or `.then` rejection handler, or is not explicitly ignored using the `void` operator.
  
  i This rule belongs to the nursery group, which means it is not yet stable and may change in the future. Visit https://biomejs.dev/linter/#nursery for more information.
  

```
