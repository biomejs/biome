---
source: crates/biome_js_analyze/tests/spec_tests.rs
expression: truthyGuardNarrowingInvalid.ts
---
# Input
```ts
// should generate diagnostics

// Narrowing must keep the promise, so the diagnostic must survive.
function narrowedToPromise(x: Promise<void> | undefined) {
	if (x) {
		x;
	}
}

```

# Diagnostics
```
truthyGuardNarrowingInvalid.ts:6:3 lint/nursery/noFloatingPromises ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  i A "floating" Promise was found, meaning it is not properly handled and could lead to ignored errors or unexpected behavior.
  
    4 │ function narrowedToPromise(x: Promise<void> | undefined) {
    5 │ 	if (x) {
  > 6 │ 		x;
      │ 		^^
    7 │ 	}
    8 │ }
  
  i This happens when a Promise is not awaited, lacks a `.catch` or `.then` rejection handler, or is not explicitly ignored using the `void` operator.
  
  i This rule belongs to the nursery group, which means it is not yet stable and may change in the future. Visit https://biomejs.dev/linter/#nursery for more information.
  

```
