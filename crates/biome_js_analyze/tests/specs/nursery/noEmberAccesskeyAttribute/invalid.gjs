// Basic accesskey attribute on HTML element
import Component from '@glimmer/component';

export default class MyButton extends Component {
  <template>
    <button accesskey="s">Save</button>
  </template>
}
