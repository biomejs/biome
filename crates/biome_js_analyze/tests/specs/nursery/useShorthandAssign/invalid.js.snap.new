---
source: crates/biome_js_analyze/tests/spec_tests.rs
assertion_line: 83
expression: invalid.js
---
# Input
```js

// a = 1 + a;
// a = a + 1;

// a = 1 - a;
// a = a - 1;

// a = 1 * a;
// a = a * 1;

// a = 1 / a;
// a = a / 1;

// a = 1 % a;
// a = a % 1;

// a = 1 << a;
// a = a << 1;

// a = 1 >> a;
// a = a >> 1;

// a = 1 >>> a;
// a = a >>> 1;

// a = 1 & a;
// a = a & 1;

// a = 1 ^ a;
// a = a ^ 1;

// a = 1 | a;
// a = a | 1;

// a = 1 ** a;
// a = a ** 1;


// this.a = this.a + 1;
// this.a = this.a + 1;

// this.a = this.a + b

// a = (a) + 1;
x = (x + y);

// x[0] = x[0] - y

```


