---
source: crates/biome_js_analyze/tests/spec_tests.rs
assertion_line: 86
expression: invalid.js
---
# Input
```jsx

if (a) {
	if (b) foo()
}
;[].forEach(bar)

```

# Diagnostics
```
invalid.js:2:9 lint/nursery/useCollapsedIf  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This if statement can be collapsed into another if statement.
  
  > 2 │ if (a) {
      │         
  > 3 │ 	if (b) foo()
      │ 	^^^^^^^^^^^^
    4 │ }
    5 │ ;[].forEach(bar)
  
  i Safe fix: Use collapsed if instead.
  
    1 1 │   
    2   │ - if·(a)·{
    3   │ - → if·(b)·foo()
    4   │ - }
      2 │ + if·(a&&b)·b;
    5 3 │   ;[].forEach(bar)
    6 4 │   
  

```
