---
source: crates/biome_js_analyze/tests/spec_tests.rs
expression: invalid.js
---
# Input
```jsx
foo.substr()
foo?.substr()
foo.bar?.substring()
foo?.[0]?.substring()
foo.bar.substr?.()
foo.bar?.substring?.()
foo.bar?.baz?.substr()
foo.bar?.baz.substring()
foo.bar.baz?.substr()
"foo".substr()
"foo".substr(bar.length, Math.min(baz, 100)) // "foo".slice(bar.length, bar.length + Math.min(baz, 100))
"foo".substr(1, "abc".length) // "foo".slice(1, 1 + "abc".length)
"foo".substr("1", 2)
"foo".substring(length, 0) // "foo".slice(0, Math.max(0, length))
foo.substring(start) // foo.slice(Math.max(0, start))
foo.substring(start, end)
"foo".substring(1, 3)
// Extra arguments
foo.substring(1, 2, 3)

```

# Diagnostics
```
invalid.js:1:5 lint/nursery/noSubstr  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Avoid using substr and consider using slice instead.
  
  > 1 │ foo.substr()
      │     ^^^^^^
    2 │ foo?.substr()
    3 │ foo.bar?.substring()
  
  i Use .substr() instead of .slice().
  
  i Safe fix: Replace inconsistent string function substr with slice.
  
     1    │ - foo.substr()
        1 │ + foo.slice()
     2  2 │   foo?.substr()
     3  3 │   foo.bar?.substring()
  

```

```
invalid.js:2:6 lint/nursery/noSubstr  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Avoid using substr and consider using slice instead.
  
    1 │ foo.substr()
  > 2 │ foo?.substr()
      │      ^^^^^^
    3 │ foo.bar?.substring()
    4 │ foo?.[0]?.substring()
  
  i Use .substr() instead of .slice().
  
  i Safe fix: Replace inconsistent string function substr with slice.
  
     1  1 │   foo.substr()
     2    │ - foo?.substr()
        2 │ + foo?.slice()
     3  3 │   foo.bar?.substring()
     4  4 │   foo?.[0]?.substring()
  

```

```
invalid.js:3:10 lint/nursery/noSubstr  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Avoid using substring and consider using slice instead.
  
    1 │ foo.substr()
    2 │ foo?.substr()
  > 3 │ foo.bar?.substring()
      │          ^^^^^^^^^
    4 │ foo?.[0]?.substring()
    5 │ foo.bar.substr?.()
  
  i Use .substring() instead of .slice().
  
  i Safe fix: Replace inconsistent string function substring with slice.
  
     1  1 │   foo.substr()
     2  2 │   foo?.substr()
     3    │ - foo.bar?.substring()
        3 │ + foo.bar?.slice()
     4  4 │   foo?.[0]?.substring()
     5  5 │   foo.bar.substr?.()
  

```

```
invalid.js:4:11 lint/nursery/noSubstr  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Avoid using substring and consider using slice instead.
  
    2 │ foo?.substr()
    3 │ foo.bar?.substring()
  > 4 │ foo?.[0]?.substring()
      │           ^^^^^^^^^
    5 │ foo.bar.substr?.()
    6 │ foo.bar?.substring?.()
  
  i Use .substring() instead of .slice().
  
  i Safe fix: Replace inconsistent string function substring with slice.
  
     2  2 │   foo?.substr()
     3  3 │   foo.bar?.substring()
     4    │ - foo?.[0]?.substring()
        4 │ + foo?.[0]?.slice()
     5  5 │   foo.bar.substr?.()
     6  6 │   foo.bar?.substring?.()
  

```

```
invalid.js:5:9 lint/nursery/noSubstr  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Avoid using substr and consider using slice instead.
  
    3 │ foo.bar?.substring()
    4 │ foo?.[0]?.substring()
  > 5 │ foo.bar.substr?.()
      │         ^^^^^^
    6 │ foo.bar?.substring?.()
    7 │ foo.bar?.baz?.substr()
  
  i Use .substr() instead of .slice().
  
  i Safe fix: Replace inconsistent string function substr with slice.
  
     3  3 │   foo.bar?.substring()
     4  4 │   foo?.[0]?.substring()
     5    │ - foo.bar.substr?.()
        5 │ + foo.bar.slice?.()
     6  6 │   foo.bar?.substring?.()
     7  7 │   foo.bar?.baz?.substr()
  

```

```
invalid.js:6:10 lint/nursery/noSubstr  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Avoid using substring and consider using slice instead.
  
    4 │ foo?.[0]?.substring()
    5 │ foo.bar.substr?.()
  > 6 │ foo.bar?.substring?.()
      │          ^^^^^^^^^
    7 │ foo.bar?.baz?.substr()
    8 │ foo.bar?.baz.substring()
  
  i Use .substring() instead of .slice().
  
  i Safe fix: Replace inconsistent string function substring with slice.
  
     4  4 │   foo?.[0]?.substring()
     5  5 │   foo.bar.substr?.()
     6    │ - foo.bar?.substring?.()
        6 │ + foo.bar?.slice?.()
     7  7 │   foo.bar?.baz?.substr()
     8  8 │   foo.bar?.baz.substring()
  

```

```
invalid.js:7:15 lint/nursery/noSubstr  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Avoid using substr and consider using slice instead.
  
    5 │ foo.bar.substr?.()
    6 │ foo.bar?.substring?.()
  > 7 │ foo.bar?.baz?.substr()
      │               ^^^^^^
    8 │ foo.bar?.baz.substring()
    9 │ foo.bar.baz?.substr()
  
  i Use .substr() instead of .slice().
  
  i Safe fix: Replace inconsistent string function substr with slice.
  
     5  5 │   foo.bar.substr?.()
     6  6 │   foo.bar?.substring?.()
     7    │ - foo.bar?.baz?.substr()
        7 │ + foo.bar?.baz?.slice()
     8  8 │   foo.bar?.baz.substring()
     9  9 │   foo.bar.baz?.substr()
  

```

```
invalid.js:8:14 lint/nursery/noSubstr  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Avoid using substring and consider using slice instead.
  
     6 │ foo.bar?.substring?.()
     7 │ foo.bar?.baz?.substr()
   > 8 │ foo.bar?.baz.substring()
       │              ^^^^^^^^^
     9 │ foo.bar.baz?.substr()
    10 │ "foo".substr()
  
  i Use .substring() instead of .slice().
  
  i Safe fix: Replace inconsistent string function substring with slice.
  
     6  6 │   foo.bar?.substring?.()
     7  7 │   foo.bar?.baz?.substr()
     8    │ - foo.bar?.baz.substring()
        8 │ + foo.bar?.baz.slice()
     9  9 │   foo.bar.baz?.substr()
    10 10 │   "foo".substr()
  

```

```
invalid.js:9:14 lint/nursery/noSubstr  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Avoid using substr and consider using slice instead.
  
     7 │ foo.bar?.baz?.substr()
     8 │ foo.bar?.baz.substring()
   > 9 │ foo.bar.baz?.substr()
       │              ^^^^^^
    10 │ "foo".substr()
    11 │ "foo".substr(bar.length, Math.min(baz, 100)) // "foo".slice(bar.length, bar.length + Math.min(baz, 100))
  
  i Use .substr() instead of .slice().
  
  i Safe fix: Replace inconsistent string function substr with slice.
  
     7  7 │   foo.bar?.baz?.substr()
     8  8 │   foo.bar?.baz.substring()
     9    │ - foo.bar.baz?.substr()
        9 │ + foo.bar.baz?.slice()
    10 10 │   "foo".substr()
    11 11 │   "foo".substr(bar.length, Math.min(baz, 100)) // "foo".slice(bar.length, bar.length + Math.min(baz, 100))
  

```

```
invalid.js:10:7 lint/nursery/noSubstr  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Avoid using substr and consider using slice instead.
  
     8 │ foo.bar?.baz.substring()
     9 │ foo.bar.baz?.substr()
  > 10 │ "foo".substr()
       │       ^^^^^^
    11 │ "foo".substr(bar.length, Math.min(baz, 100)) // "foo".slice(bar.length, bar.length + Math.min(baz, 100))
    12 │ "foo".substr(1, "abc".length) // "foo".slice(1, 1 + "abc".length)
  
  i Use .substr() instead of .slice().
  
  i Safe fix: Replace inconsistent string function substr with slice.
  
     8  8 │   foo.bar?.baz.substring()
     9  9 │   foo.bar.baz?.substr()
    10    │ - "foo".substr()
       10 │ + "foo".slice()
    11 11 │   "foo".substr(bar.length, Math.min(baz, 100)) // "foo".slice(bar.length, bar.length + Math.min(baz, 100))
    12 12 │   "foo".substr(1, "abc".length) // "foo".slice(1, 1 + "abc".length)
  

```

```
invalid.js:11:7 lint/nursery/noSubstr ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Avoid using substr and consider using slice instead.
  
     9 │ foo.bar.baz?.substr()
    10 │ "foo".substr()
  > 11 │ "foo".substr(bar.length, Math.min(baz, 100)) // "foo".slice(bar.length, bar.length + Math.min(baz, 100))
       │       ^^^^^^
    12 │ "foo".substr(1, "abc".length) // "foo".slice(1, 1 + "abc".length)
    13 │ "foo".substr("1", 2)
  
  i Use .substr() instead of .slice().
  

```

```
invalid.js:12:7 lint/nursery/noSubstr ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Avoid using substr and consider using slice instead.
  
    10 │ "foo".substr()
    11 │ "foo".substr(bar.length, Math.min(baz, 100)) // "foo".slice(bar.length, bar.length + Math.min(baz, 100))
  > 12 │ "foo".substr(1, "abc".length) // "foo".slice(1, 1 + "abc".length)
       │       ^^^^^^
    13 │ "foo".substr("1", 2)
    14 │ "foo".substring(length, 0) // "foo".slice(0, Math.max(0, length))
  
  i Use .substr() instead of .slice().
  

```

```
invalid.js:13:7 lint/nursery/noSubstr ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Avoid using substr and consider using slice instead.
  
    11 │ "foo".substr(bar.length, Math.min(baz, 100)) // "foo".slice(bar.length, bar.length + Math.min(baz, 100))
    12 │ "foo".substr(1, "abc".length) // "foo".slice(1, 1 + "abc".length)
  > 13 │ "foo".substr("1", 2)
       │       ^^^^^^
    14 │ "foo".substring(length, 0) // "foo".slice(0, Math.max(0, length))
    15 │ foo.substring(start) // foo.slice(Math.max(0, start))
  
  i Use .substr() instead of .slice().
  

```

```
invalid.js:14:7 lint/nursery/noSubstr ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Avoid using substring and consider using slice instead.
  
    12 │ "foo".substr(1, "abc".length) // "foo".slice(1, 1 + "abc".length)
    13 │ "foo".substr("1", 2)
  > 14 │ "foo".substring(length, 0) // "foo".slice(0, Math.max(0, length))
       │       ^^^^^^^^^
    15 │ foo.substring(start) // foo.slice(Math.max(0, start))
    16 │ foo.substring(start, end)
  
  i Use .substring() instead of .slice().
  

```

```
invalid.js:15:5 lint/nursery/noSubstr ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Avoid using substring and consider using slice instead.
  
    13 │ "foo".substr("1", 2)
    14 │ "foo".substring(length, 0) // "foo".slice(0, Math.max(0, length))
  > 15 │ foo.substring(start) // foo.slice(Math.max(0, start))
       │     ^^^^^^^^^
    16 │ foo.substring(start, end)
    17 │ "foo".substring(1, 3)
  
  i Use .substring() instead of .slice().
  

```

```
invalid.js:16:5 lint/nursery/noSubstr ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Avoid using substring and consider using slice instead.
  
    14 │ "foo".substring(length, 0) // "foo".slice(0, Math.max(0, length))
    15 │ foo.substring(start) // foo.slice(Math.max(0, start))
  > 16 │ foo.substring(start, end)
       │     ^^^^^^^^^
    17 │ "foo".substring(1, 3)
    18 │ // Extra arguments
  
  i Use .substring() instead of .slice().
  

```

```
invalid.js:17:7 lint/nursery/noSubstr ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Avoid using substring and consider using slice instead.
  
    15 │ foo.substring(start) // foo.slice(Math.max(0, start))
    16 │ foo.substring(start, end)
  > 17 │ "foo".substring(1, 3)
       │       ^^^^^^^^^
    18 │ // Extra arguments
    19 │ foo.substring(1, 2, 3)
  
  i Use .substring() instead of .slice().
  

```

```
invalid.js:19:5 lint/nursery/noSubstr ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Avoid using substring and consider using slice instead.
  
    17 │ "foo".substring(1, 3)
    18 │ // Extra arguments
  > 19 │ foo.substring(1, 2, 3)
       │     ^^^^^^^^^
    20 │ 
  
  i Use .substring() instead of .slice().
  

```
