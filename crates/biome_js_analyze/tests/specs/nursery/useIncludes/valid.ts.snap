---
source: crates/biome_js_analyze/tests/spec_tests.rs
expression: valid.ts
---
# Input
```ts
/* should not generate diagnostics */

const collectionWithIncludes = {
    items: [1, 2, 3],
    includes(searchElement: number) {
        return this.items.includes(searchElement);
    },
    indexOf(searchElement: number) {
        return this.items.indexOf(searchElement);
    }
};
collectionWithIncludes.includes(2);

const collectionWithoutIncludes = {
    items: [1, 2, 3],
    indexOf(searchElement: number) {
        return this.items.indexOf(searchElement);
    }
};
collectionWithoutIncludes.indexOf(2) !== -1;

// Regex with flags other than 'u' or 'v' are not simple
/a/g.test("abc");

// Regex with special characters are not simple
/a/i.test("abc");

// Regex with character class is not simple
/[ab]/.test("abc");

```
