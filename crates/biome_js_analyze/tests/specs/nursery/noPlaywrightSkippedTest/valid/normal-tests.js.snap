---
source: crates/biome_js_analyze/tests/spec_tests.rs
expression: normal-tests.js
---
# Input
```js
/* should not generate diagnostics */

// Normal test
test("working test", async ({ page }) => {
    await page.click("button");
});

// Normal it
it("working it", async ({ page }) => {
    await page.click("button");
});

// Normal describe
test.describe("suite", () => {
    test("test", async ({ page }) => {
        await page.click("button");
    });
});

// test.only (different concern, not covered by this rule)
test.only("focused test", async ({ page }) => {
    await page.click("button");
});

// test.describe.only
test.describe.only("focused suite", () => {
    test("test", async ({ page }) => {});
});

// test.describe.parallel
test.describe.parallel("parallel suite", () => {
    test("test", async ({ page }) => {});
});

// test.describe.serial
test.describe.serial("serial suite", () => {
    test("test", async ({ page }) => {});
});

// Normal step
test("test with step", async ({ page }) => {
    await test.step("step", async () => {
        await page.click("button");
    });
});

```
