/* should not generate diagnostics */

// Platform-agnostic components are fine regardless of custom options
import { View, Text, ScrollView } from "react-native";

const { View: MyView } = require("react-native");
