---
source: crates/biome_js_analyze/tests/spec_tests.rs
expression: invalid-unimported-toRefs.js
---
# Input
```js
// toRefs not imported should be invalid
export default {
  setup(props) {
    const { foo } = toRefs(props);
    return () => h('div', foo.value)
  }
}
```

# Diagnostics
```
invalid-unimported-toRefs.js:4:11 lint/nursery/noVueSetupPropsReactivityLoss ━━━━━━━━━━━━━━━━━━━━━━━

  i Destructuring `props` in the root scope of `setup` loses reactivity.
  
    2 │ export default {
    3 │   setup(props) {
  > 4 │     const { foo } = toRefs(props);
      │           ^^^^^^^
    5 │     return () => h('div', foo.value)
    6 │   }
  
  i The `props` parameter is defined here.
  
    1 │ // toRefs not imported should be invalid
    2 │ export default {
  > 3 │   setup(props) {
      │         ^^^^^
    4 │     const { foo } = toRefs(props);
    5 │     return () => h('div', foo.value)
  
  i To preserve reactivity, access props as properties: `props.propertyName`.
  
  i Alternatively, use `toRefs(props)` or `toRef(props, 'key')` to create reactive refs.
  

```
