---
source: crates/biome_js_analyze/tests/spec_tests.rs
expression: invalidGenericTypeAlias.ts
---
# Input
```ts
// `MaybePromise<string>` substitutes to `string | Promise<string>`, so a Promise reaches each condition.
type MaybePromise<T> = T | Promise<T>;
declare const cached: MaybePromise<string>;
if (cached) {
}
const label = cached ? "ready" : "pending";
while (cached) {
}

```

# Diagnostics
```
invalidGenericTypeAlias.ts:4:5 lint/nursery/noMisusedPromises ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  i A Promise was found where a conditional was expected.
  
    2 │ type MaybePromise<T> = T | Promise<T>;
    3 │ declare const cached: MaybePromise<string>;
  > 4 │ if (cached) {
      │     ^^^^^^
    5 │ }
    6 │ const label = cached ? "ready" : "pending";
  
  i A Promise is always truthy, so this is most likely a mistake.
  
  i You may have intended to `await` the Promise instead.
  
  i This rule belongs to the nursery group, which means it is not yet stable and may change in the future. Visit https://biomejs.dev/linter/#nursery for more information.
  

```

```
invalidGenericTypeAlias.ts:6:15 lint/nursery/noMisusedPromises ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  i A Promise was found where a conditional was expected.
  
    4 │ if (cached) {
    5 │ }
  > 6 │ const label = cached ? "ready" : "pending";
      │               ^^^^^^
    7 │ while (cached) {
    8 │ }
  
  i A Promise is always truthy, so this is most likely a mistake.
  
  i You may have intended to `await` the Promise instead.
  
  i This rule belongs to the nursery group, which means it is not yet stable and may change in the future. Visit https://biomejs.dev/linter/#nursery for more information.
  

```

```
invalidGenericTypeAlias.ts:7:8 lint/nursery/noMisusedPromises ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  i A Promise was found where a conditional was expected.
  
    5 │ }
    6 │ const label = cached ? "ready" : "pending";
  > 7 │ while (cached) {
      │        ^^^^^^
    8 │ }
    9 │ 
  
  i A Promise is always truthy, so this is most likely a mistake.
  
  i You may have intended to `await` the Promise instead.
  
  i This rule belongs to the nursery group, which means it is not yet stable and may change in the future. Visit https://biomejs.dev/linter/#nursery for more information.
  

```
