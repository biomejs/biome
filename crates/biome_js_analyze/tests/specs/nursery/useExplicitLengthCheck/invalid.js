foo.length!=0
/** A */ 0 /** B */ != /** D */ foo.length /** E */
if (/** A */foo.size /** B */) {}
// Boolean(foo.length)
// new Boolean(foo.length)
// !foo.length
// !this.length
// true ? foo.length : 0
// true ? foo.length == 0 : 0

// !!!(!!foo.length)