---
source: crates/biome_js_analyze/tests/spec_tests.rs
assertion_line: 83
expression: invalid.jsonc
---
# Input
```js
new RegExp('abc');
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp('abc');
      │ ^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·RegExp('abc');
  + /abc/;
  

```

# Input
```js
new RegExp((('abc')), ('g'));
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp((('abc')), ('g'));
      │ ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·RegExp((('abc')),·('g'));
  + /abc/g;
  

```

# Input
```js
RegExp('abc');
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp('abc');
      │ ^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp('abc');
  + /abc/;
  

```

# Input
```js
new RegExp('abc', 'g');
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp('abc', 'g');
      │ ^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·RegExp('abc',·'g');
  + /abc/g;
  

```

# Input
```js
RegExp('abc', 'g');
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp('abc', 'g');
      │ ^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp('abc',·'g');
  + /abc/g;
  

```

# Input
```js
new RegExp(`abc`);
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp(`abc`);
      │ ^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·RegExp(`abc`);
  + /abc/;
  

```

# Input
```js
RegExp(`abc`);
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp(`abc`);
      │ ^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp(`abc`);
  + /abc/;
  

```

# Input
```js
new RegExp(`abc`, `g`);
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp(`abc`, `g`);
      │ ^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·RegExp(`abc`,·`g`);
  + /abc/g;
  

```

# Input
```js
RegExp(`abc`, `g`);
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp(`abc`, `g`);
      │ ^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp(`abc`,·`g`);
  + /abc/g;
  

```

# Input
```js
new RegExp(String.raw`abc`);
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp(String.raw`abc`);
      │ ^^^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·RegExp(String.raw`abc`);
  + /abc/;
  

```

# Input
```js
new RegExp(String.raw`abc
abc`);
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp(String.raw`abc
      │ ^^^^^^^^^^^^^^^^^^^^^^^^^
  > 2 │ abc`);
      │ ^^^^^
  
  i Safe fix: Use a literal notation instead.
  
    1   │ - new·RegExp(String.raw`abc
    2   │ - abc`);
      1 │ + /abc\nabc/;
  

```

# Input
```js
new RegExp(String.raw`	abc
abc`);
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp(String.raw`	abc
      │ ^^^^^^^^^^^^^^^^^^^^^^^^^^^
  > 2 │ abc`);
      │ ^^^^^
  
  i Safe fix: Use a literal notation instead.
  
    1   │ - new·RegExp(String.raw`→ abc
    2   │ - abc`);
      1 │ + /→ abc\nabc/;
  

```

# Input
```js
RegExp(String.raw`abc`);
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp(String.raw`abc`);
      │ ^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp(String.raw`abc`);
  + /abc/;
  

```

# Input
```js
new RegExp(String.raw`abc`, String.raw`g`);
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp(String.raw`abc`, String.raw`g`);
      │ ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·RegExp(String.raw`abc`,·String.raw`g`);
  + /abc/g;
  

```

# Input
```js
RegExp(String.raw`abc`, String.raw`g`);
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp(String.raw`abc`, String.raw`g`);
      │ ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp(String.raw`abc`,·String.raw`g`);
  + /abc/g;
  

```

# Input
```js
new RegExp(String['raw']`a`);
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp(String['raw']`a`);
      │ ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·RegExp(String['raw']`a`);
  + /a/;
  

```

# Input
```js
new RegExp('a', `g`);
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp('a', `g`);
      │ ^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·RegExp('a',·`g`);
  + /a/g;
  

```

# Input
```js
RegExp(`a`, 'g');
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp(`a`, 'g');
      │ ^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp(`a`,·'g');
  + /a/g;
  

```

# Input
```js
RegExp(String.raw`a`, 'g');
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp(String.raw`a`, 'g');
      │ ^^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp(String.raw`a`,·'g');
  + /a/g;
  

```

# Input
```js
new RegExp(String.raw`\d`, `g`);
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp(String.raw`\d`, `g`);
      │ ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·RegExp(String.raw`\d`,·`g`);
  + /\d/g;
  

```

# Input
```js
new RegExp(String.raw`\\d`, `g`);
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp(String.raw`\\d`, `g`);
      │ ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·RegExp(String.raw`\\d`,·`g`);
  + /\d/g;
  

```

# Input
```js
new RegExp(String['raw']`\\d`, `g`);
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp(String['raw']`\\d`, `g`);
      │ ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·RegExp(String['raw']`\\d`,·`g`);
  + /\d/g;
  

```

# Input
```js
new RegExp(String["raw"]`\\d`, `g`);
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp(String["raw"]`\\d`, `g`);
      │ ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·RegExp(String["raw"]`\\d`,·`g`);
  + /\d/g;
  

```

# Input
```js
RegExp('a', String.raw`g`);
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp('a', String.raw`g`);
      │ ^^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp('a',·String.raw`g`);
  + /a/g;
  

```

# Input
```js
new globalThis.RegExp('a');
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new globalThis.RegExp('a');
      │ ^^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·globalThis.RegExp('a');
  + /a/;
  

```

# Input
```js
globalThis.RegExp('a');
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ globalThis.RegExp('a');
      │ ^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - globalThis.RegExp('a');
  + /a/;
  

```

# Input
```js
(globalThis).RegExp('a');
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ (globalThis).RegExp('a');
      │ ^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - (globalThis).RegExp('a');
  + /a/;
  

```

# Input
```js
new RegExp((String?.raw)`a`);
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp((String?.raw)`a`);
      │ ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·RegExp((String?.raw)`a`);
  + /a/;
  

```

# Input
```js
RegExp('abc', 'u');
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp('abc', 'u');
      │ ^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp('abc',·'u');
  + /abc/u;
  

```

# Input
```js
new RegExp('abc', 'd');
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp('abc', 'd');
      │ ^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·RegExp('abc',·'d');
  + /abc/d;
  

```

# Input
```js
RegExp('abc', 'd');
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp('abc', 'd');
      │ ^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp('abc',·'d');
  + /abc/d;
  

```

# Input
```js
RegExp('\\\\', '');
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp('\\\\', '');
      │ ^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp('\\\\',·'');
  + /\\/;
  

```

# Input
```js
RegExp('\n', '');
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp('\n', '');
      │ ^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp('\n',·'');
  + /\n/;
  

```

# Input
```js
RegExp('\n\n', '');
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp('\n\n', '');
      │ ^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp('\n\n',·'');
  + /\n\n/;
  

```

# Input
```js
RegExp('\t', '');
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp('\t', '');
      │ ^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp('\t',·'');
  + /\t/;
  

```

# Input
```js
RegExp('\t\t', '');
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp('\t\t', '');
      │ ^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp('\t\t',·'');
  + /\t\t/;
  

```

# Input
```js
RegExp('\r\n', '');
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp('\r\n', '');
      │ ^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp('\r\n',·'');
  + /\r\n/;
  

```

# Input
```js
RegExp('\u1234', 'g')
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp('\u1234', 'g')
      │ ^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp('\u1234',·'g')
  + /\u1234/g
  

```

# Input
```js
RegExp('\u{1234}', 'g')
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp('\u{1234}', 'g')
      │ ^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp('\u{1234}',·'g')
  + /\u{1234}/g
  

```

# Input
```js
RegExp('\u{11111}', 'g')
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp('\u{11111}', 'g')
      │ ^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp('\u{11111}',·'g')
  + /\u{11111}/g
  

```

# Input
```js
RegExp('\v', '');
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp('\v', '');
      │ ^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp('\v',·'');
  + /\v/;
  

```

# Input
```js
RegExp('\v\v', '');
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp('\v\v', '');
      │ ^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp('\v\v',·'');
  + /\v\v/;
  

```

# Input
```js
RegExp('\f', '');
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp('\f', '');
      │ ^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp('\f',·'');
  + /\f/;
  

```

# Input
```js
RegExp('\f\f', '');
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp('\f\f', '');
      │ ^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp('\f\f',·'');
  + /\f\f/;
  

```

# Input
```js
RegExp('\\b', '');
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp('\\b', '');
      │ ^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp('\\b',·'');
  + /\b/;
  

```

# Input
```js
RegExp('\\b\\b', '');
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp('\\b\\b', '');
      │ ^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp('\\b\\b',·'');
  + /\b\b/;
  

```

# Input
```js
new RegExp('\\B\\b', '');
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp('\\B\\b', '');
      │ ^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·RegExp('\\B\\b',·'');
  + /\B\b/;
  

```

# Input
```js
RegExp('\\w', '');
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp('\\w', '');
      │ ^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp('\\w',·'');
  + /\w/;
  

```

# Input
```js
new globalThis.RegExp('\\W', '');
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new globalThis.RegExp('\\W', '');
      │ ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·globalThis.RegExp('\\W',·'');
  + /\W/;
  

```

# Input
```js
RegExp('\\s', '');
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp('\\s', '');
      │ ^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp('\\s',·'');
  + /\s/;
  

```

# Input
```js
new RegExp('\\S', '')
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp('\\S', '')
      │ ^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·RegExp('\\S',·'')
  + /\S/
  

```

# Input
```js
globalThis.RegExp('\\d', '');
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ globalThis.RegExp('\\d', '');
      │ ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - globalThis.RegExp('\\d',·'');
  + /\d/;
  

```

# Input
```js
globalThis.RegExp('\\D', '')
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ globalThis.RegExp('\\D', '')
      │ ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - globalThis.RegExp('\\D',·'')
  + /\D/
  

```

# Input
```js
globalThis.RegExp('\\\\\\D', '')
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ globalThis.RegExp('\\\\\\D', '')
      │ ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - globalThis.RegExp('\\\\\\D',·'')
  + /\\\D/
  

```

# Input
```js
new RegExp('\\D\\D', '')
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp('\\D\\D', '')
      │ ^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·RegExp('\\D\\D',·'')
  + /\D\D/
  

```

# Input
```js
new globalThis.RegExp('\\0\\0', '');
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new globalThis.RegExp('\\0\\0', '');
      │ ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·globalThis.RegExp('\\0\\0',·'');
  + /\0\0/;
  

```

# Input
```js
new RegExp('\\0\\0', '');
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp('\\0\\0', '');
      │ ^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·RegExp('\\0\\0',·'');
  + /\0\0/;
  

```

# Input
```js
new RegExp('\0\0', 'g');
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp('\0\0', 'g');
      │ ^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·RegExp('\0\0',·'g');
  + /\0\0/g;
  

```

# Input
```js
RegExp('\\0\\0\\0', '')
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp('\\0\\0\\0', '')
      │ ^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp('\\0\\0\\0',·'')
  + /\0\0\0/
  

```

# Input
```js
RegExp('\\78\\126\\5934', '')
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp('\\78\\126\\5934', '')
      │ ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp('\\78\\126\\5934',·'')
  + /\78\126\5934/
  

```

# Input
```js
new window['RegExp']('\\x56\\x78\\x45', '');
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new window['RegExp']('\\x56\\x78\\x45', '');
      │ ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·window['RegExp']('\\x56\\x78\\x45',·'');
  + /\x56\x78\x45/;
  

```

# Input
```js
new (window['RegExp'])('\\x56\\x78\\x45', '');
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new (window['RegExp'])('\\x56\\x78\\x45', '');
      │ ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·(window['RegExp'])('\\x56\\x78\\x45',·'');
  + /\x56\x78\x45/;
  

```

# Input
```js
a in(RegExp('abc'))
```

# Diagnostics
```
invalid.jsonc:1:6 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ a in(RegExp('abc'))
      │      ^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - a·in(RegExp('abc'))
  + a·in(/abc/)
  

```

# Input
```js
func(new RegExp(String.raw`\w{1, 2`, 'u'),new RegExp(String.raw`\w{1, 2`, 'u'))
```

# Diagnostics
```
invalid.jsonc:1:6 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ func(new RegExp(String.raw`\w{1, 2`, 'u'),new RegExp(String.raw`\w{1, 2`, 'u'))
      │      ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - func(new·RegExp(String.raw`\w{1,·2`,·'u'),new·RegExp(String.raw`\w{1,·2`,·'u'))
  + func(/\w{1,·2/u,new·RegExp(String.raw`\w{1,·2`,·'u'))
  

```

```
invalid.jsonc:1:43 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ func(new RegExp(String.raw`\w{1, 2`, 'u'),new RegExp(String.raw`\w{1, 2`, 'u'))
      │                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - func(new·RegExp(String.raw`\w{1,·2`,·'u'),new·RegExp(String.raw`\w{1,·2`,·'u'))
  + func(new·RegExp(String.raw`\w{1,·2`,·'u'),/\w{1,·2/u)
  

```

# Input
```js
x = y;
            RegExp("foo").test(x) ? bar() : baz()
```

# Diagnostics
```
invalid.jsonc:2:13 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
    1 │ x = y;
  > 2 │             RegExp("foo").test(x) ? bar() : baz()
      │             ^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   x = y;
    2   │ - ············RegExp("foo").test(x)·?·bar()·:·baz()
      2 │ + ············/foo/.test(x)·?·bar()·:·baz()
  

```

# Input
```js
typeof RegExp("foo")
```

# Diagnostics
```
invalid.jsonc:1:8 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ typeof RegExp("foo")
      │        ^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - typeof·RegExp("foo")
  + typeof·/foo/
  

```

# Input
```js
RegExp("foo") instanceof RegExp(String.raw`blahblah`, 'g') ? typeof new RegExp('(\\p{Emoji_Presentation})\\1', `ug`) : false
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp("foo") instanceof RegExp(String.raw`blahblah`, 'g') ? typeof new RegExp('(\\p{Emoji_Presentation})\\1', `ug`) : false
      │ ^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp("foo")·instanceof·RegExp(String.raw`blahblah`,·'g')·?·typeof·new·RegExp('(\\p{Emoji_Presentation})\\1',·`ug`)·:·false
  + /foo/·instanceof·RegExp(String.raw`blahblah`,·'g')·?·typeof·new·RegExp('(\\p{Emoji_Presentation})\\1',·`ug`)·:·false
  

```

```
invalid.jsonc:1:26 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp("foo") instanceof RegExp(String.raw`blahblah`, 'g') ? typeof new RegExp('(\\p{Emoji_Presentation})\\1', `ug`) : false
      │                          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp("foo")·instanceof·RegExp(String.raw`blahblah`,·'g')·?·typeof·new·RegExp('(\\p{Emoji_Presentation})\\1',·`ug`)·:·false
  + RegExp("foo")·instanceof·/blahblah/g·?·typeof·new·RegExp('(\\p{Emoji_Presentation})\\1',·`ug`)·:·false
  

```

```
invalid.jsonc:1:69 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp("foo") instanceof RegExp(String.raw`blahblah`, 'g') ? typeof new RegExp('(\\p{Emoji_Presentation})\\1', `ug`) : false
      │                                                                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp("foo")·instanceof·RegExp(String.raw`blahblah`,·'g')·?·typeof·new·RegExp('(\\p{Emoji_Presentation})\\1',·`ug`)·:·false
  + RegExp("foo")·instanceof·RegExp(String.raw`blahblah`,·'g')·?·typeof·/(\p{Emoji_Presentation})\1/ug·:·false
  

```

# Input
```js
[   new RegExp(`someregular`)]
```

# Diagnostics
```
invalid.jsonc:1:5 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ [   new RegExp(`someregular`)]
      │     ^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - [···new·RegExp(`someregular`)]
  + [···/someregular/]
  

```

# Input
```js
const totallyValidatesEmails = new RegExp("\\S+@(\\S+\\.)+\\S+")
            if (typeof totallyValidatesEmails === 'object') {
                runSomethingThatExists(Regexp('stuff'))
            }
```

# Diagnostics
```
invalid.jsonc:1:32 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ const totallyValidatesEmails = new RegExp("\\S+@(\\S+\\.)+\\S+")
      │                                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    2 │             if (typeof totallyValidatesEmails === 'object') {
    3 │                 runSomethingThatExists(Regexp('stuff'))
  
  i Safe fix: Use a literal notation instead.
  
    1   │ - const·totallyValidatesEmails·=·new·RegExp("\\S+@(\\S+\\.)+\\S+")
      1 │ + const·totallyValidatesEmails·=·/\S+@(\S+\.)+\S+/
    2 2 │               if (typeof totallyValidatesEmails === 'object') {
    3 3 │                   runSomethingThatExists(Regexp('stuff'))
  

```

# Input
```js
!new RegExp('^Hey, ', 'u') && new RegExp('jk$') && ~new RegExp('^Sup, ') || new RegExp('hi') + new RegExp('person') === -new RegExp('hi again') ? 5 * new RegExp('abc') : 'notregbutstring'
```

# Diagnostics
```
invalid.jsonc:1:2 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ !new RegExp('^Hey, ', 'u') && new RegExp('jk$') && ~new RegExp('^Sup, ') || new RegExp('hi') + new RegExp('person') === -new RegExp('hi again') ? 5 * new RegExp('abc') : 'notregbutstring'
      │  ^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - !new·RegExp('^Hey,·',·'u')·&&·new·RegExp('jk$')·&&·~new·RegExp('^Sup,·')·||·new·RegExp('hi')·+·new·RegExp('person')·===·-new·RegExp('hi·again')·?·5·*·new·RegExp('abc')·:·'notregbutstring'
  + !/^Hey,·/u·&&·new·RegExp('jk$')·&&·~new·RegExp('^Sup,·')·||·new·RegExp('hi')·+·new·RegExp('person')·===·-new·RegExp('hi·again')·?·5·*·new·RegExp('abc')·:·'notregbutstring'
  

```

```
invalid.jsonc:1:31 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ !new RegExp('^Hey, ', 'u') && new RegExp('jk$') && ~new RegExp('^Sup, ') || new RegExp('hi') + new RegExp('person') === -new RegExp('hi again') ? 5 * new RegExp('abc') : 'notregbutstring'
      │                               ^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - !new·RegExp('^Hey,·',·'u')·&&·new·RegExp('jk$')·&&·~new·RegExp('^Sup,·')·||·new·RegExp('hi')·+·new·RegExp('person')·===·-new·RegExp('hi·again')·?·5·*·new·RegExp('abc')·:·'notregbutstring'
  + !new·RegExp('^Hey,·',·'u')·&&·/jk$/·&&·~new·RegExp('^Sup,·')·||·new·RegExp('hi')·+·new·RegExp('person')·===·-new·RegExp('hi·again')·?·5·*·new·RegExp('abc')·:·'notregbutstring'
  

```

```
invalid.jsonc:1:53 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ !new RegExp('^Hey, ', 'u') && new RegExp('jk$') && ~new RegExp('^Sup, ') || new RegExp('hi') + new RegExp('person') === -new RegExp('hi again') ? 5 * new RegExp('abc') : 'notregbutstring'
      │                                                     ^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - !new·RegExp('^Hey,·',·'u')·&&·new·RegExp('jk$')·&&·~new·RegExp('^Sup,·')·||·new·RegExp('hi')·+·new·RegExp('person')·===·-new·RegExp('hi·again')·?·5·*·new·RegExp('abc')·:·'notregbutstring'
  + !new·RegExp('^Hey,·',·'u')·&&·new·RegExp('jk$')·&&·~/^Sup,·/·||·new·RegExp('hi')·+·new·RegExp('person')·===·-new·RegExp('hi·again')·?·5·*·new·RegExp('abc')·:·'notregbutstring'
  

```

```
invalid.jsonc:1:77 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ !new RegExp('^Hey, ', 'u') && new RegExp('jk$') && ~new RegExp('^Sup, ') || new RegExp('hi') + new RegExp('person') === -new RegExp('hi again') ? 5 * new RegExp('abc') : 'notregbutstring'
      │                                                                             ^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - !new·RegExp('^Hey,·',·'u')·&&·new·RegExp('jk$')·&&·~new·RegExp('^Sup,·')·||·new·RegExp('hi')·+·new·RegExp('person')·===·-new·RegExp('hi·again')·?·5·*·new·RegExp('abc')·:·'notregbutstring'
  + !new·RegExp('^Hey,·',·'u')·&&·new·RegExp('jk$')·&&·~new·RegExp('^Sup,·')·||·/hi/·+·new·RegExp('person')·===·-new·RegExp('hi·again')·?·5·*·new·RegExp('abc')·:·'notregbutstring'
  

```

```
invalid.jsonc:1:96 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ !new RegExp('^Hey, ', 'u') && new RegExp('jk$') && ~new RegExp('^Sup, ') || new RegExp('hi') + new RegExp('person') === -new RegExp('hi again') ? 5 * new RegExp('abc') : 'notregbutstring'
      │                                                                                                ^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - !new·RegExp('^Hey,·',·'u')·&&·new·RegExp('jk$')·&&·~new·RegExp('^Sup,·')·||·new·RegExp('hi')·+·new·RegExp('person')·===·-new·RegExp('hi·again')·?·5·*·new·RegExp('abc')·:·'notregbutstring'
  + !new·RegExp('^Hey,·',·'u')·&&·new·RegExp('jk$')·&&·~new·RegExp('^Sup,·')·||·new·RegExp('hi')·+·/person/·===·-new·RegExp('hi·again')·?·5·*·new·RegExp('abc')·:·'notregbutstring'
  

```

```
invalid.jsonc:1:122 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ !new RegExp('^Hey, ', 'u') && new RegExp('jk$') && ~new RegExp('^Sup, ') || new RegExp('hi') + new RegExp('person') === -new RegExp('hi again') ? 5 * new RegExp('abc') : 'notregbutstring'
      │                                                                                                                          ^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - !new·RegExp('^Hey,·',·'u')·&&·new·RegExp('jk$')·&&·~new·RegExp('^Sup,·')·||·new·RegExp('hi')·+·new·RegExp('person')·===·-new·RegExp('hi·again')·?·5·*·new·RegExp('abc')·:·'notregbutstring'
  + !new·RegExp('^Hey,·',·'u')·&&·new·RegExp('jk$')·&&·~new·RegExp('^Sup,·')·||·new·RegExp('hi')·+·new·RegExp('person')·===·-/hi·again/·?·5·*·new·RegExp('abc')·:·'notregbutstring'
  

```

```
invalid.jsonc:1:151 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ !new RegExp('^Hey, ', 'u') && new RegExp('jk$') && ~new RegExp('^Sup, ') || new RegExp('hi') + new RegExp('person') === -new RegExp('hi again') ? 5 * new RegExp('abc') : 'notregbutstring'
      │                                                                                                                                                       ^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - !new·RegExp('^Hey,·',·'u')·&&·new·RegExp('jk$')·&&·~new·RegExp('^Sup,·')·||·new·RegExp('hi')·+·new·RegExp('person')·===·-new·RegExp('hi·again')·?·5·*·new·RegExp('abc')·:·'notregbutstring'
  + !new·RegExp('^Hey,·',·'u')·&&·new·RegExp('jk$')·&&·~new·RegExp('^Sup,·')·||·new·RegExp('hi')·+·new·RegExp('person')·===·-new·RegExp('hi·again')·?·5·*·/abc/·:·'notregbutstring'
  

```

# Input
```js
#!/usr/bin/sh
            RegExp("foo")
```

# Diagnostics
```
invalid.jsonc:2:13 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
    1 │ #!/usr/bin/sh
  > 2 │             RegExp("foo")
      │             ^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   #!/usr/bin/sh
    2   │ - ············RegExp("foo")
      2 │ + ············/foo/
  

```

# Input
```js
async function abc(){await new RegExp("foo")}
```

# Diagnostics
```
invalid.jsonc:1:28 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ async function abc(){await new RegExp("foo")}
      │                            ^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - async·function·abc(){await·new·RegExp("foo")}
  + async·function·abc(){await·/foo/}
  

```

# Input
```js
function* abc(){yield new RegExp("foo")}
```

# Diagnostics
```
invalid.jsonc:1:23 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ function* abc(){yield new RegExp("foo")}
      │                       ^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - function*·abc(){yield·new·RegExp("foo")}
  + function*·abc(){yield·/foo/}
  

```

# Input
```js
function* abc(){yield* new RegExp("foo")}
```

# Diagnostics
```
invalid.jsonc:1:24 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ function* abc(){yield* new RegExp("foo")}
      │                        ^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - function*·abc(){yield*·new·RegExp("foo")}
  + function*·abc(){yield*·/foo/}
  

```

# Input
```js
console.log({ ...new RegExp('a') })
```

# Diagnostics
```
invalid.jsonc:1:18 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ console.log({ ...new RegExp('a') })
      │                  ^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - console.log({·...new·RegExp('a')·})
  + console.log({·.../a/·})
  

```

# Input
```js
delete RegExp('a');
```

# Diagnostics
```
invalid.jsonc:1:8 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ delete RegExp('a');
      │        ^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - delete·RegExp('a');
  + delete·/a/;
  

```

# Input
```js
void RegExp('a');
```

# Diagnostics
```
invalid.jsonc:1:6 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ void RegExp('a');
      │      ^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - void·RegExp('a');
  + void·/a/;
  

```

# Input
```js
new RegExp("\\S+@(\\S+\\.)+\\S+")**RegExp('a')
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp("\\S+@(\\S+\\.)+\\S+")**RegExp('a')
      │ ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·RegExp("\\S+@(\\S+\\.)+\\S+")**RegExp('a')
  + /\S+@(\S+\.)+\S+/**RegExp('a')
  

```

```
invalid.jsonc:1:36 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp("\\S+@(\\S+\\.)+\\S+")**RegExp('a')
      │                                    ^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·RegExp("\\S+@(\\S+\\.)+\\S+")**RegExp('a')
  + new·RegExp("\\S+@(\\S+\\.)+\\S+")**/a/
  

```

# Input
```js
new RegExp("\\S+@(\\S+\\.)+\\S+")%RegExp('a')
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp("\\S+@(\\S+\\.)+\\S+")%RegExp('a')
      │ ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·RegExp("\\S+@(\\S+\\.)+\\S+")%RegExp('a')
  + /\S+@(\S+\.)+\S+/%RegExp('a')
  

```

```
invalid.jsonc:1:35 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp("\\S+@(\\S+\\.)+\\S+")%RegExp('a')
      │                                   ^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·RegExp("\\S+@(\\S+\\.)+\\S+")%RegExp('a')
  + new·RegExp("\\S+@(\\S+\\.)+\\S+")%/a/
  

```

# Input
```js
a in RegExp('abc')
```

# Diagnostics
```
invalid.jsonc:1:6 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ a in RegExp('abc')
      │      ^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - a·in·RegExp('abc')
  + a·in·/abc/
  

```

# Input
```js

            /abc/ == new RegExp('cba');
            
```

# Diagnostics
```
invalid.jsonc:2:22 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 2 │             /abc/ == new RegExp('cba');
      │                      ^^^^^^^^^^^^^^^^^
    3 │             
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   
    2   │ - ············/abc/·==·new·RegExp('cba');
      2 │ + ············/abc/·==·/cba/;
    3 3 │               
  

```

# Input
```js

            /abc/ === new RegExp('cba');
            
```

# Diagnostics
```
invalid.jsonc:2:23 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 2 │             /abc/ === new RegExp('cba');
      │                       ^^^^^^^^^^^^^^^^^
    3 │             
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   
    2   │ - ············/abc/·===·new·RegExp('cba');
      2 │ + ············/abc/·===·/cba/;
    3 3 │               
  

```

# Input
```js

            /abc/ != new RegExp('cba');
            
```

# Diagnostics
```
invalid.jsonc:2:22 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 2 │             /abc/ != new RegExp('cba');
      │                      ^^^^^^^^^^^^^^^^^
    3 │             
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   
    2   │ - ············/abc/·!=·new·RegExp('cba');
      2 │ + ············/abc/·!=·/cba/;
    3 3 │               
  

```

# Input
```js

            /abc/ !== new RegExp('cba');
            
```

# Diagnostics
```
invalid.jsonc:2:23 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 2 │             /abc/ !== new RegExp('cba');
      │                       ^^^^^^^^^^^^^^^^^
    3 │             
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   
    2   │ - ············/abc/·!==·new·RegExp('cba');
      2 │ + ············/abc/·!==·/cba/;
    3 3 │               
  

```

# Input
```js

            /abc/ > new RegExp('cba');
            
```

# Diagnostics
```
invalid.jsonc:2:21 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 2 │             /abc/ > new RegExp('cba');
      │                     ^^^^^^^^^^^^^^^^^
    3 │             
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   
    2   │ - ············/abc/·>·new·RegExp('cba');
      2 │ + ············/abc/·>·/cba/;
    3 3 │               
  

```

# Input
```js

            /abc/ < new RegExp('cba');
            
```

# Diagnostics
```
invalid.jsonc:2:21 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 2 │             /abc/ < new RegExp('cba');
      │                     ^^^^^^^^^^^^^^^^^
    3 │             
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   
    2   │ - ············/abc/·<·new·RegExp('cba');
      2 │ + ············/abc/·<·/cba/;
    3 3 │               
  

```

# Input
```js

            /abc/ >= new RegExp('cba');
            
```

# Diagnostics
```
invalid.jsonc:2:22 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 2 │             /abc/ >= new RegExp('cba');
      │                      ^^^^^^^^^^^^^^^^^
    3 │             
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   
    2   │ - ············/abc/·>=·new·RegExp('cba');
      2 │ + ············/abc/·>=·/cba/;
    3 3 │               
  

```

# Input
```js

            /abc/ <= new RegExp('cba');
            
```

# Diagnostics
```
invalid.jsonc:2:22 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 2 │             /abc/ <= new RegExp('cba');
      │                      ^^^^^^^^^^^^^^^^^
    3 │             
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   
    2   │ - ············/abc/·<=·new·RegExp('cba');
      2 │ + ············/abc/·<=·/cba/;
    3 3 │               
  

```

# Input
```js

            /abc/ << new RegExp('cba');
            
```

# Diagnostics
```
invalid.jsonc:2:22 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 2 │             /abc/ << new RegExp('cba');
      │                      ^^^^^^^^^^^^^^^^^
    3 │             
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   
    2   │ - ············/abc/·<<·new·RegExp('cba');
      2 │ + ············/abc/·<<·/cba/;
    3 3 │               
  

```

# Input
```js

            /abc/ >> new RegExp('cba');
            
```

# Diagnostics
```
invalid.jsonc:2:22 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 2 │             /abc/ >> new RegExp('cba');
      │                      ^^^^^^^^^^^^^^^^^
    3 │             
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   
    2   │ - ············/abc/·>>·new·RegExp('cba');
      2 │ + ············/abc/·>>·/cba/;
    3 3 │               
  

```

# Input
```js

            /abc/ >>> new RegExp('cba');
            
```

# Diagnostics
```
invalid.jsonc:2:23 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 2 │             /abc/ >>> new RegExp('cba');
      │                       ^^^^^^^^^^^^^^^^^
    3 │             
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   
    2   │ - ············/abc/·>>>·new·RegExp('cba');
      2 │ + ············/abc/·>>>·/cba/;
    3 3 │               
  

```

# Input
```js

            /abc/ ^ new RegExp('cba');
            
```

# Diagnostics
```
invalid.jsonc:2:21 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 2 │             /abc/ ^ new RegExp('cba');
      │                     ^^^^^^^^^^^^^^^^^
    3 │             
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   
    2   │ - ············/abc/·^·new·RegExp('cba');
      2 │ + ············/abc/·^·/cba/;
    3 3 │               
  

```

# Input
```js

            /abc/ & new RegExp('cba');
            
```

# Diagnostics
```
invalid.jsonc:2:21 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 2 │             /abc/ & new RegExp('cba');
      │                     ^^^^^^^^^^^^^^^^^
    3 │             
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   
    2   │ - ············/abc/·&·new·RegExp('cba');
      2 │ + ············/abc/·&·/cba/;
    3 3 │               
  

```

# Input
```js

            /abc/ | new RegExp('cba');
            
```

# Diagnostics
```
invalid.jsonc:2:21 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 2 │             /abc/ | new RegExp('cba');
      │                     ^^^^^^^^^^^^^^^^^
    3 │             
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   
    2   │ - ············/abc/·|·new·RegExp('cba');
      2 │ + ············/abc/·|·/cba/;
    3 3 │               
  

```

# Input
```js

            null ?? new RegExp('blah')
            
```

# Diagnostics
```
invalid.jsonc:2:21 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 2 │             null ?? new RegExp('blah')
      │                     ^^^^^^^^^^^^^^^^^^
    3 │             
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   
    2   │ - ············null·??·new·RegExp('blah')
      2 │ + ············null·??·/blah/
    3 3 │               
  

```

# Input
```js

            abc *= new RegExp('blah')
            
```

# Diagnostics
```
invalid.jsonc:2:20 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 2 │             abc *= new RegExp('blah')
      │                    ^^^^^^^^^^^^^^^^^^
    3 │             
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   
    2   │ - ············abc·*=·new·RegExp('blah')
      2 │ + ············abc·*=·/blah/
    3 3 │               
  

```

# Input
```js

            console.log({a: new RegExp('sup')})
            
```

# Diagnostics
```
invalid.jsonc:2:29 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 2 │             console.log({a: new RegExp('sup')})
      │                             ^^^^^^^^^^^^^^^^^
    3 │             
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   
    2   │ - ············console.log({a:·new·RegExp('sup')})
      2 │ + ············console.log({a:·/sup/})
    3 3 │               
  

```

# Input
```js

            console.log(() => {new RegExp('sup')})
            
```

# Diagnostics
```
invalid.jsonc:2:32 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 2 │             console.log(() => {new RegExp('sup')})
      │                                ^^^^^^^^^^^^^^^^^
    3 │             
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   
    2   │ - ············console.log(()·=>·{new·RegExp('sup')})
      2 │ + ············console.log(()·=>·{/sup/})
    3 3 │               
  

```

# Input
```js

            function abc() {new RegExp('sup')}
            
```

# Diagnostics
```
invalid.jsonc:2:29 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 2 │             function abc() {new RegExp('sup')}
      │                             ^^^^^^^^^^^^^^^^^
    3 │             
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   
    2   │ - ············function·abc()·{new·RegExp('sup')}
      2 │ + ············function·abc()·{/sup/}
    3 3 │               
  

```

# Input
```js

            function abc() {return new RegExp('sup')}
            
```

# Diagnostics
```
invalid.jsonc:2:36 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 2 │             function abc() {return new RegExp('sup')}
      │                                    ^^^^^^^^^^^^^^^^^
    3 │             
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   
    2   │ - ············function·abc()·{return·new·RegExp('sup')}
      2 │ + ············function·abc()·{return·/sup/}
    3 3 │               
  

```

# Input
```js

            abc <<= new RegExp('cba');
            
```

# Diagnostics
```
invalid.jsonc:2:21 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 2 │             abc <<= new RegExp('cba');
      │                     ^^^^^^^^^^^^^^^^^
    3 │             
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   
    2   │ - ············abc·<<=·new·RegExp('cba');
      2 │ + ············abc·<<=·/cba/;
    3 3 │               
  

```

# Input
```js

            abc >>= new RegExp('cba');
            
```

# Diagnostics
```
invalid.jsonc:2:21 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 2 │             abc >>= new RegExp('cba');
      │                     ^^^^^^^^^^^^^^^^^
    3 │             
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   
    2   │ - ············abc·>>=·new·RegExp('cba');
      2 │ + ············abc·>>=·/cba/;
    3 3 │               
  

```

# Input
```js

            abc >>>= new RegExp('cba');
            
```

# Diagnostics
```
invalid.jsonc:2:22 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 2 │             abc >>>= new RegExp('cba');
      │                      ^^^^^^^^^^^^^^^^^
    3 │             
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   
    2   │ - ············abc·>>>=·new·RegExp('cba');
      2 │ + ············abc·>>>=·/cba/;
    3 3 │               
  

```

# Input
```js

            abc ^= new RegExp('cba');
            
```

# Diagnostics
```
invalid.jsonc:2:20 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 2 │             abc ^= new RegExp('cba');
      │                    ^^^^^^^^^^^^^^^^^
    3 │             
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   
    2   │ - ············abc·^=·new·RegExp('cba');
      2 │ + ············abc·^=·/cba/;
    3 3 │               
  

```

# Input
```js

            abc &= new RegExp('cba');
            
```

# Diagnostics
```
invalid.jsonc:2:20 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 2 │             abc &= new RegExp('cba');
      │                    ^^^^^^^^^^^^^^^^^
    3 │             
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   
    2   │ - ············abc·&=·new·RegExp('cba');
      2 │ + ············abc·&=·/cba/;
    3 3 │               
  

```

# Input
```js

            abc |= new RegExp('cba');
            
```

# Diagnostics
```
invalid.jsonc:2:20 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 2 │             abc |= new RegExp('cba');
      │                    ^^^^^^^^^^^^^^^^^
    3 │             
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   
    2   │ - ············abc·|=·new·RegExp('cba');
      2 │ + ············abc·|=·/cba/;
    3 3 │               
  

```

# Input
```js

            abc ??= new RegExp('cba');
            
```

# Diagnostics
```
invalid.jsonc:2:21 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 2 │             abc ??= new RegExp('cba');
      │                     ^^^^^^^^^^^^^^^^^
    3 │             
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   
    2   │ - ············abc·??=·new·RegExp('cba');
      2 │ + ············abc·??=·/cba/;
    3 3 │               
  

```

# Input
```js

            abc &&= new RegExp('cba');
            
```

# Diagnostics
```
invalid.jsonc:2:21 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 2 │             abc &&= new RegExp('cba');
      │                     ^^^^^^^^^^^^^^^^^
    3 │             
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   
    2   │ - ············abc·&&=·new·RegExp('cba');
      2 │ + ············abc·&&=·/cba/;
    3 3 │               
  

```

# Input
```js

            abc ||= new RegExp('cba');
            
```

# Diagnostics
```
invalid.jsonc:2:21 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 2 │             abc ||= new RegExp('cba');
      │                     ^^^^^^^^^^^^^^^^^
    3 │             
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   
    2   │ - ············abc·||=·new·RegExp('cba');
      2 │ + ············abc·||=·/cba/;
    3 3 │               
  

```

# Input
```js

            abc **= new RegExp('blah')
            
```

# Diagnostics
```
invalid.jsonc:2:21 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 2 │             abc **= new RegExp('blah')
      │                     ^^^^^^^^^^^^^^^^^^
    3 │             
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   
    2   │ - ············abc·**=·new·RegExp('blah')
      2 │ + ············abc·**=·/blah/
    3 3 │               
  

```

# Input
```js

            abc /= new RegExp('blah')
            
```

# Diagnostics
```
invalid.jsonc:2:20 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 2 │             abc /= new RegExp('blah')
      │                    ^^^^^^^^^^^^^^^^^^
    3 │             
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   
    2   │ - ············abc·/=·new·RegExp('blah')
      2 │ + ············abc·/=·/blah/
    3 3 │               
  

```

# Input
```js

            abc += new RegExp('blah')
            
```

# Diagnostics
```
invalid.jsonc:2:20 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 2 │             abc += new RegExp('blah')
      │                    ^^^^^^^^^^^^^^^^^^
    3 │             
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   
    2   │ - ············abc·+=·new·RegExp('blah')
      2 │ + ············abc·+=·/blah/
    3 3 │               
  

```

# Input
```js

            abc -= new RegExp('blah')
            
```

# Diagnostics
```
invalid.jsonc:2:20 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 2 │             abc -= new RegExp('blah')
      │                    ^^^^^^^^^^^^^^^^^^
    3 │             
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   
    2   │ - ············abc·-=·new·RegExp('blah')
      2 │ + ············abc·-=·/blah/
    3 3 │               
  

```

# Input
```js

            abc %= new RegExp('blah')
            
```

# Diagnostics
```
invalid.jsonc:2:20 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 2 │             abc %= new RegExp('blah')
      │                    ^^^^^^^^^^^^^^^^^^
    3 │             
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   
    2   │ - ············abc·%=·new·RegExp('blah')
      2 │ + ············abc·%=·/blah/
    3 3 │               
  

```

# Input
```js

            () => new RegExp('blah')
            
```

# Diagnostics
```
invalid.jsonc:2:19 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 2 │             () => new RegExp('blah')
      │                   ^^^^^^^^^^^^^^^^^^
    3 │             
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   
    2   │ - ············()·=>·new·RegExp('blah')
      2 │ + ············()·=>·/blah/
    3 3 │               
  

```

# Input
```js
a/RegExp("foo")in b
```

# Diagnostics
```
invalid.jsonc:1:3 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ a/RegExp("foo")in b
      │   ^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - a/RegExp("foo")in·b
  + a//foo/in·b
  

```

# Input
```js
a/RegExp("foo")instanceof b
```

# Diagnostics
```
invalid.jsonc:1:3 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ a/RegExp("foo")instanceof b
      │   ^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - a/RegExp("foo")instanceof·b
  + a//foo/instanceof·b
  

```

# Input
```js
do RegExp("foo")
while (true);
```

# Diagnostics
```
invalid.jsonc:1:4 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ do RegExp("foo")
      │    ^^^^^^^^^^^^^
    2 │ while (true);
  
  i Safe fix: Use a literal notation instead.
  
    1   │ - do·RegExp("foo")
      1 │ + do·/foo/
    2 2 │   while (true);
  

```

# Input
```js
for(let i;i<5;i++) { break
new RegExp('search')}
```

# Diagnostics
```
invalid.jsonc:2:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
    1 │ for(let i;i<5;i++) { break
  > 2 │ new RegExp('search')}
      │ ^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   for(let i;i<5;i++) { break
    2   │ - new·RegExp('search')}
      2 │ + /search/}
  

```

# Input
```js
for(let i;i<5;i++) { continue
new RegExp('search')}
```

# Diagnostics
```
invalid.jsonc:2:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
    1 │ for(let i;i<5;i++) { continue
  > 2 │ new RegExp('search')}
      │ ^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   for(let i;i<5;i++) { continue
    2   │ - new·RegExp('search')}
      2 │ + /search/}
  

```

# Input
```js

            switch (value) {
                case "possibility":
                    console.log('possibility matched')
                case RegExp('myReg').toString():
                    console.log('matches a regexp\' toString value')
                    break;
            }
            
```

# Diagnostics
```
invalid.jsonc:5:22 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
    3 │                 case "possibility":
    4 │                     console.log('possibility matched')
  > 5 │                 case RegExp('myReg').toString():
      │                      ^^^^^^^^^^^^^^^
    6 │                     console.log('matches a regexp\' toString value')
    7 │                     break;
  
  i Safe fix: Use a literal notation instead.
  
    3 3 │                   case "possibility":
    4 4 │                       console.log('possibility matched')
    5   │ - ················case·RegExp('myReg').toString():
      5 │ + ················case·/myReg/.toString():
    6 6 │                       console.log('matches a regexp\' toString value')
    7 7 │                       break;
  

```

# Input
```js
throw new RegExp('abcdefg') // fail with a regular expression
```

# Diagnostics
```
invalid.jsonc:1:7 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ throw new RegExp('abcdefg') // fail with a regular expression
      │       ^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - throw·new·RegExp('abcdefg')·//·fail·with·a·regular·expression
  + throw·/abcdefg/·//·fail·with·a·regular·expression
  

```

# Input
```js
for (value of new RegExp('something being searched')) { console.log(value) }
```

# Diagnostics
```
invalid.jsonc:1:15 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ for (value of new RegExp('something being searched')) { console.log(value) }
      │               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - for·(value·of·new·RegExp('something·being·searched'))·{·console.log(value)·}
  + for·(value·of·/something·being·searched/)·{·console.log(value)·}
  

```

# Input
```js
(async function(){for await (value of new RegExp('something being searched')) { console.log(value) }})()
```

# Diagnostics
```
invalid.jsonc:1:39 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ (async function(){for await (value of new RegExp('something being searched')) { console.log(value) }})()
      │                                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - (async·function(){for·await·(value·of·new·RegExp('something·being·searched'))·{·console.log(value)·}})()
  + (async·function(){for·await·(value·of·/something·being·searched/)·{·console.log(value)·}})()
  

```

# Input
```js
for (value in new RegExp('something being searched')) { console.log(value) }
```

# Diagnostics
```
invalid.jsonc:1:15 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ for (value in new RegExp('something being searched')) { console.log(value) }
      │               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - for·(value·in·new·RegExp('something·being·searched'))·{·console.log(value)·}
  + for·(value·in·/something·being·searched/)·{·console.log(value)·}
  

```

# Input
```js
if (condition1 && condition2) new RegExp('avalue').test(str);
```

# Diagnostics
```
invalid.jsonc:1:31 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ if (condition1 && condition2) new RegExp('avalue').test(str);
      │                               ^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - if·(condition1·&&·condition2)·new·RegExp('avalue').test(str);
  + if·(condition1·&&·condition2)·/avalue/.test(str);
  

```

# Input
```js
debugger
new RegExp('myReg')
```

# Diagnostics
```
invalid.jsonc:2:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
    1 │ debugger
  > 2 │ new RegExp('myReg')
      │ ^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
    1 1 │   debugger
    2   │ - new·RegExp('myReg')
      2 │ + /myReg/
  

```

# Input
```js
RegExp("\\\n")
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp("\\\n")
      │ ^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp("\\\n")
  + /\\n/
  

```

# Input
```js
RegExp("\\\t")
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp("\\\t")
      │ ^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp("\\\t")
  + /\\t/
  

```

# Input
```js
RegExp("\\\f")
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp("\\\f")
      │ ^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp("\\\f")
  + /\\f/
  

```

# Input
```js
RegExp("\\\v")
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp("\\\v")
      │ ^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp("\\\v")
  + /\\v/
  

```

# Input
```js
RegExp("\\\r")
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ RegExp("\\\r")
      │ ^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - RegExp("\\\r")
  + /\\r/
  

```

# Input
```js
new RegExp("	")
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp("	")
      │ ^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·RegExp("→ ")
  + /→ /
  

```

# Input
```js
new RegExp("/")
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp("/")
      │ ^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·RegExp("/")
  + ///
  

```

# Input
```js
new RegExp("\.")
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp("\.")
      │ ^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·RegExp("\.")
  + /\./
  

```

# Input
```js
new RegExp("\\.")
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp("\\.")
      │ ^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·RegExp("\\.")
  + /\./
  

```

# Input
```js
new RegExp("\\\n\\\n")
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp("\\\n\\\n")
      │ ^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·RegExp("\\\n\\\n")
  + /\\n\\n/
  

```

# Input
```js
new RegExp("\\\n\\\f\\\n")
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp("\\\n\\\f\\\n")
      │ ^^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·RegExp("\\\n\\\f\\\n")
  + /\\n\\f\\n/
  

```

# Input
```js
new RegExp("\u000A\u000A");
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp("\u000A\u000A");
      │ ^^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·RegExp("\u000A\u000A");
  + /\u000A\u000A/;
  

```

# Input
```js
new RegExp('mysafereg' /* comment explaining its safety */)
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp('mysafereg' /* comment explaining its safety */)
      │ ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·RegExp('mysafereg'·/*·comment·explaining·its·safety·*/)
  + /mysafereg/
  

```

# Input
```js
new RegExp('[[A--B]]', 'v')
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp('[[A--B]]', 'v')
      │ ^^^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·RegExp('[[A--B]]',·'v')
  + /[[A--B]]/v
  

```

# Input
```js
new RegExp('[[A--B]]', 'v')
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp('[[A--B]]', 'v')
      │ ^^^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·RegExp('[[A--B]]',·'v')
  + /[[A--B]]/v
  

```

# Input
```js
new RegExp('[[A&&&]]', 'v')
```

# Diagnostics
```
invalid.jsonc:1:1 lint/nursery/useRegexLiterals  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Use a regular expression literal instead of the RegExp constructor.
  
  > 1 │ new RegExp('[[A&&&]]', 'v')
      │ ^^^^^^^^^^^^^^^^^^^^^^^^^^^
  
  i Safe fix: Use a literal notation instead.
  
  - new·RegExp('[[A&&&]]',·'v')
  + /[[A&&&]]/v
  

```


