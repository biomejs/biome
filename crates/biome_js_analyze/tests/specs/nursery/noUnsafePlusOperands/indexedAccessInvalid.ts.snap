---
source: crates/biome_js_analyze/tests/spec_tests.rs
expression: indexedAccessInvalid.ts
---
# Input
```ts
/* should generate diagnostics */

const counts = [1, 2, 3] as const;
declare const count: (typeof counts)[number];
count + 1n;

declare const bigint: bigint[][number];
bigint + 1;

```

# Diagnostics
```
indexedAccessInvalid.ts:5:1 lint/nursery/noUnsafePlusOperands ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  i Numeric + operations must use either two bigint values or two number values.
  
    3 │ const counts = [1, 2, 3] as const;
    4 │ declare const count: (typeof counts)[number];
  > 5 │ count + 1n;
      │ ^^^^^^^^^^
    6 │ 
    7 │ declare const bigint: bigint[][number];
  
  i This operation mixes union with bigint.
  
    3 │ const counts = [1, 2, 3] as const;
    4 │ declare const count: (typeof counts)[number];
  > 5 │ count + 1n;
      │ ^^^^^^^^^^
    6 │ 
    7 │ declare const bigint: bigint[][number];
  
  i Convert one side so both operands use the same numeric type before applying + or +=.
  
  i This rule belongs to the nursery group, which means it is not yet stable and may change in the future. Visit https://biomejs.dev/linter/#nursery for more information.
  

```

```
indexedAccessInvalid.ts:8:1 lint/nursery/noUnsafePlusOperands ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  i Numeric + operations must use either two bigint values or two number values.
  
    7 │ declare const bigint: bigint[][number];
  > 8 │ bigint + 1;
      │ ^^^^^^^^^^
    9 │ 
  
  i This operation mixes number with bigint.
  
    7 │ declare const bigint: bigint[][number];
  > 8 │ bigint + 1;
      │ ^^^^^^^^^^
    9 │ 
  
  i Convert one side so both operands use the same numeric type before applying + or +=.
  
  i This rule belongs to the nursery group, which means it is not yet stable and may change in the future. Visit https://biomejs.dev/linter/#nursery for more information.
  

```
