---
source: crates/biome_js_analyze/tests/spec_tests.rs
expression: valid.ts
---
# Input
```ts
/* should not generate diagnostics */
enum Valid1 {
	A = 0,
	B = 1,
}

enum Valid2 {
	A = "A",
	B = 'B',
	C = `C`,
}

enum Valid3 {
	A = 0,
	B = "0",
}

enum Valid4 {
	A = 0,
	B = 1,
	C = 0.5,
	D,
}

```
