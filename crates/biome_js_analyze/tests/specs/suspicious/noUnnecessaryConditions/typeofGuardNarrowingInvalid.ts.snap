---
source: crates/biome_js_analyze/tests/spec_tests.rs
expression: typeofGuardNarrowingInvalid.ts
---
# Input
```ts
// should generate diagnostics

function alwaysTruthyAfterGuard(x: number | (() => void)) {
	if (typeof x === "function") {
		// `x` is narrowed to a function here, so this condition is always truthy.
		if (x) {
			x();
		}
	}
}

```

# Diagnostics
```
typeofGuardNarrowingInvalid.ts:6:7 lint/suspicious/noUnnecessaryConditions ━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This condition is always truthy.
  
    4 │ 	if (typeof x === "function") {
    5 │ 		// `x` is narrowed to a function here, so this condition is always truthy.
  > 6 │ 		if (x) {
      │ 		    ^
    7 │ 			x();
    8 │ 		}
  
  i The value's type can never be falsy, so this check is redundant.
  

```
