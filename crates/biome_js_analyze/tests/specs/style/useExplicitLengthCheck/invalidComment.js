function useQueryAssistant() {
  if (
    !showThinking && // while it's thinking there is hope to get suggestions
    !comments?.length
  ) {
    console.log();
  }
}

const isEmpty =
  // leading comment
  !foo.length;

const isNotEmpty =
  Boolean(foo.length);

if (
  // check length
  foo.length
) {}

const isEmptyToo =
  // check length
  foo.length < 1;

while (
  bar ||
  // own line
  foo.length
) {}
