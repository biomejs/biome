/* comment */Math.pow(a, b)
Math.pow(a, b)/* comment */;
Math.pow(a, b)// comment

Math.pow(/**/a/**/, /**/b/**/)

Math/**/.pow/**/(a, b)


Math // 0
    .pow( // 1
        // 2
        a, // 3
        // 4
        b, // 5
    // 6
    ) // 5
