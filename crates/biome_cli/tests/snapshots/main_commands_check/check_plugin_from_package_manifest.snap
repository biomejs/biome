---
source: crates/biome_cli/tests/snap_test.rs
expression: redactor(content)
---
## `biome.json`

```json
{
  "plugins": ["@scope/biome-plugin"],
  "formatter": { "enabled": false }
}
```

## `file.js`

```js
Object.assign({});
```

## `node_modules/@scope/biome-plugin/biome-manifest.json`

```json
{
    "version": 1,
    "rules": ["rules/noAssign.grit"]
}
```

## `node_modules/@scope/biome-plugin/package.json`

```json
{
    "name": "@scope/biome-plugin",
    "exports": "./index.js"
}
```

## `node_modules/@scope/biome-plugin/rules/noAssign.grit`

```grit
`Object.assign($args)` where {
    register_diagnostic(
        span = $args,
        message = "Prefer object spread instead of `Object.assign()`"
    )
}
```

# Termination Message

```block
check ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  × Some errors were emitted while running checks.
  


```

# Emitted Messages

```block
file.js:1:15 plugin ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  × Prefer object spread instead of `Object.assign()`
  
  > 1 │ Object.assign({});
      │               ^^
  

```

```block
Checked 1 file in <TIME>. No fixes applied.
Found 1 error.
```
