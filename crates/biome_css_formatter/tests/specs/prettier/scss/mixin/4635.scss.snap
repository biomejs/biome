---
source: crates/biome_formatter_test/src/snapshot_builder.rs
info: scss/mixin/4635.scss
---

# Input

```scss

@mixin placeholder {
  &::placeholder {@content}
}

.container {
  @include placeholder {
    color: $color-silver;
  }
}

```


# Prettier differences

```diff
--- Prettier
+++ Biome
@@ -1,6 +1,6 @@
 @mixin placeholder {
   &::placeholder {
-    @content;
+    @content
   }
 }
 
```

# Output

```scss
@mixin placeholder {
  &::placeholder {
    @content
  }
}

.container {
  @include placeholder {
    color: $color-silver;
  }
}
```

# Errors
```
4635.scss:3:27 parse ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  × expected `;` but instead found `}`
  
    2 │ @mixin placeholder {
  > 3 │   &::placeholder {@content}
      │                           ^
    4 │ }
    5 │ 
  
  i Remove }
  

```
