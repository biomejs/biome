---
source: crates/biome_formatter_test/src/snapshot_builder.rs
info: css/scss/at-rule/mixin.scss
---

# Input

```scss
@mixin button{
border-radius:4px;
}

@mixin size(
$width,
$height:$width,
$args...
){
width:$width;
@if $height > 0{
height:$height;
}
}

```


# Formatted

```scss
@mixin button {
	border-radius: 4px;
}

@mixin size($width, $height: $width, $args...) {
	width: $width;
	@if $height > 0 {
		height: $height;
	}
}

```
