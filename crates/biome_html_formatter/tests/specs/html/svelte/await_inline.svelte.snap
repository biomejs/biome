---
source: crates/biome_formatter_test/src/snapshot_builder.rs
info: svelte/await_inline.svelte
---

# Input

```svelte
<div>{#await promise then value}<span>{value}</span>{/await}</div>

```


# Formatted

```svelte
<div>
	{#await promise then value}
		<span>{value}</span>
	{/await}
</div>

```
