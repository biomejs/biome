---
source: crates/biome_formatter_test/src/snapshot_builder.rs
info: svelte/each_with_index_and_key.svelte
---

# Input

```svelte
{#each items as item, i (item.id)}
  <div>{i}: {item.name}</div>
{/each}

```


# Formatted

```svelte
{#each items as item, i (item.id)}
	<div>{i}: {item.name}</div>
{/each}

```
