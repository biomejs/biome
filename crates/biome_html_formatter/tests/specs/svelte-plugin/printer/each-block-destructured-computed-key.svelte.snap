---
source: crates/biome_formatter_test/src/snapshot_builder.rs
info: printer/each-block-destructured-computed-key.svelte
---

# Input

```svelte
{#each ids as id}
    {#each objs as { [id]: value }}
        <td>{value}</td>
    {/each}
{/each}

```


# Errors
```
each-block-destructured-computed-key.svelte:2:26 parse ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  × expected `,` but instead found `:`
  
    1 │ {#each ids as id}
  > 2 │     {#each objs as { [id]: value }}
      │                          ^
    3 │         <td>{value}</td>
    4 │     {/each}
  
  i Remove :
  
each-block-destructured-computed-key.svelte:3:9 parse ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  × expected `,` but instead found `<`
  
    1 │ {#each ids as id}
    2 │     {#each objs as { [id]: value }}
  > 3 │         <td>{value}</td>
      │         ^
    4 │     {/each}
    5 │ {/each}
  
  i Remove <
  
each-block-destructured-computed-key.svelte:6:1 parse ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  × Expected a closing block, instead found none.
  
    4 │     {/each}
    5 │ {/each}
  > 6 │ 
      │ 
  
  i This is where the block started.
  
    1 │ {#each ids as id}
    2 │     {#each objs as { [id]: value }}
  > 3 │         <td>{value}</td>
      │                        
    4 │     {/each}
    5 │ {/each}
  

```

# Error
```text
format: Can't format code because it contains syntax errors
```
