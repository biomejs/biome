---
source: crates/biome_formatter_test/src/snapshot_builder.rs
info: printer/each-block-destructured-nested-rest.svelte
---

# Input

```svelte
{#each array as [first, second, ...[third, ...{ length }]]}
    <p>{first} {second} {third} {length}</p>
{/each}

{#each items as [head, ...{ length }]}
    <p>{head} {length}</p>
{/each}

```


# Errors
```
each-block-destructured-nested-rest.svelte:1:36 parse ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  × Expected a valid Svelte name after '...'
  
  > 1 │ {#each array as [first, second, ...[third, ...{ length }]]}
      │                                    ^
    2 │     <p>{first} {second} {third} {length}</p>
    3 │ {/each}
  
each-block-destructured-nested-rest.svelte:1:47 parse ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  × Expected a valid Svelte name after '...'
  
  > 1 │ {#each array as [first, second, ...[third, ...{ length }]]}
      │                                               ^
    2 │     <p>{first} {second} {third} {length}</p>
    3 │ {/each}
  
each-block-destructured-nested-rest.svelte:5:27 parse ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  × Expected a valid Svelte name after '...'
  
    3 │ {/each}
    4 │ 
  > 5 │ {#each items as [head, ...{ length }]}
      │                           ^
    6 │     <p>{head} {length}</p>
    7 │ {/each}
  

```

# Error
```text
format: Can't format code because it contains syntax errors
```
