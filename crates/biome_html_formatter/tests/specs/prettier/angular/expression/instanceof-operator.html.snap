---
source: crates/biome_formatter_test/src/snapshot_builder.rs
info: angular/expression/instanceof-operator.html
---

# Input

```html
{{

  ( (a instanceof  (b)))
}}

```


# Prettier differences

```diff
--- Prettier
+++ Biome
@@ -1 +1,4 @@
-{{ a instanceof b }}
+{{
+
+  ( (a instanceof  (b)))
+}}
```

# Output

```html
{{

  ( (a instanceof  (b)))
}}
```

# Errors
```
instanceof-operator.html:1:1 parse ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  × Text expressions aren't supported.
  
  > 1 │ {{
      │ ^^
  > 2 │ 
  > 3 │   ( (a instanceof  (b)))
  > 4 │ }}
      │ ^^
    5 │ 
  
  i Remove it or enable the parsing using the html.parser.interpolation option.
  

```
