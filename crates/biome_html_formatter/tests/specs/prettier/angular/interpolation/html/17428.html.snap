---
source: crates/biome_formatter_test/src/snapshot_builder.rs
info: angular/interpolation/html/17428.html
---

# Input

```html
(there is no {{`${activityType === '' ? 'activity' : ''}${activityType === '' && costCenter === '' ? ' or ' : ''}${costCenter === '' ? 'cost center' : ''}`}})

```


# Prettier differences

```diff
--- Prettier
+++ Biome
@@ -1,5 +1,2 @@
 (there is no
-{{
-  `${activityType === "" ? "activity" : ""}${activityType === "" && costCenter === "" ? " or " : ""}${costCenter === "" ? "cost center" : ""}`
-
-}})
+{{`${activityType === '' ? 'activity' : ''}${activityType === '' && costCenter === '' ? ' or ' : ''}${costCenter === '' ? 'cost center' : ''}`}})
```

# Output

```html
(there is no
{{`${activityType === '' ? 'activity' : ''}${activityType === '' && costCenter === '' ? ' or ' : ''}${costCenter === '' ? 'cost center' : ''}`}})
```

# Errors
```
17428.html:1:14 parse ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  × Text expressions aren't supported.
  
  > 1 │ (there is no {{`${activityType === '' ? 'activity' : ''}${activityType === '' && costCenter === '' ? ' or ' : ''}${costCenter === '' ? 'cost center' : ''}`}})
      │              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    2 │ 
  
  i Remove it or enable the parsing using the html.parser.interpolation option.
  

```

# Lines exceeding max width of 80 characters
```
    2: {{`${activityType === '' ? 'activity' : ''}${activityType === '' && costCenter === '' ? ' or ' : ''}${costCenter === '' ? 'cost center' : ''}`}})
```
