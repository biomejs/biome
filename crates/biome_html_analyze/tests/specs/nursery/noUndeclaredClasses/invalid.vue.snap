---
source: crates/biome_html_analyze/tests/spec_tests.rs
expression: invalid.vue
---
# Input
```vue
<!-- should generate diagnostics -->
<template>
	<!-- ghost-class is referenced here but never defined in any <style> block. -->
	<div class="ghost-class">Undefined class</div>

	<!-- header is defined in the global <style> block, so it's valid. -->
	<div class="header">Valid header</div>
</template>

<style>
	.header { font-size: 2rem; }
</style>

<style scoped>
	/* scoped is local — .local-only applies to this component's own elements
	   but is NOT the class being tested here. */
	.local-only { color: red; }
</style>

```

# Diagnostics
```
invalid.vue:4:14 lint/nursery/noUndeclaredClasses ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  i The CSS class ghost-class is not defined in any available stylesheet.
  
    2 │ <template>
    3 │ 	<!-- ghost-class is referenced here but never defined in any <style> block. -->
  > 4 │ 	<div class="ghost-class">Undefined class</div>
      │ 	            ^^^^^^^^^^^
    5 │ 
    6 │ 	<!-- header is defined in the global <style> block, so it's valid. -->
  
  i Referencing undefined classes often indicates a typo or a missing stylesheet, and will result in elements not being styled as intended.
  
  i Checked import tree:
    
    invalid.vue (this file)
  
  
  i Either define this class in a `<style>` block, import a CSS file that contains it, or remove this class name.
  
  i This rule is still being actively worked on, so it may be missing features or have rough edges. Visit https://github.com/biomejs/biome/issues/9156 for more information or to report possible bugs.
  
  i This rule belongs to the nursery group, which means it is not yet stable and may change in the future. Visit https://biomejs.dev/linter/#nursery for more information.
  

```
