<!-- should not generate diagnostics -->

<div set:html={html}>{}</div>
<div set:text={text}>{   }</div>
<div set:html={html}>{/* only a comment */}</div>
<div set:text={text}>{
    // only a comment
}</div>
<div set:html={html}>{/* first comment */ /* second comment */}</div>
