<Component
  dir="auto"
  id="myid"
  spellcheck="true"
  tabindex="-1"
  class:list={classes}
  client:load
  define:vars={vars}
  is:raw
  server:defer
  set:html={rawHTMLString}
  set:text={text}
/>

<Component
  class:list={classes}
  client:load
  is:raw
  set:text={text}
  {...props}
  dir="auto"
  id="myid"
  spellcheck="true"
  tabindex="-1"
  define:vars={vars}
  server:defer
  set:html={rawHTMLString}
/>
