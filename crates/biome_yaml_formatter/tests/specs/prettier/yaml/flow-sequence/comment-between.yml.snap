---
source: crates/biome_formatter_test/src/snapshot_builder.rs
info: yaml/flow-sequence/comment-between.yml
---

# Input

```yml
[
? 123
# comment
: 456
]

```


# Error
```text
reformat: output mismatch
--- re-formatted
+++ formatted
@@ -1,3 +1,4 @@
 [
-  ? 123: 456, # comment
+  ? 123: # comment
+  456,
 ]

IR differences:
--- re-formatted IR
+++ formatted IR
@@ -5,21 +5,22 @@
     indent([
       soft_line_break,
       group(expand: propagated, [
-        source_position(4),
+        source_position(2),
         "? ",
-        source_position(6),
+        source_position(4),
         "123",
-        line_suffix([" ", source_position(11), "# comment"]),
-        expand_parent,
-        source_position(9),
+        source_position(18),
         ": ",
-        source_position(23),
+        source_position(8),
+        "# comment",
+        hard_line_break,
+        source_position(20),
         "456"
       ]),
-      if_group_breaks([source_position(26), ","])
+      if_group_breaks([","])
     ]),
     soft_line_break,
-    source_position(28),
+    source_position(24),
     "]"
   ]),
   hard_line_break

```
