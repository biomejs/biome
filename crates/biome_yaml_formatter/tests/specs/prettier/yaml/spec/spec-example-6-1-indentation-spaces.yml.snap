---
source: crates/biome_formatter_test/src/snapshot_builder.rs
info: yaml/spec/spec-example-6-1-indentation-spaces.yml
---

# Input

```yml
  # Leading comment line spaces are
   # neither content nor indentation.
    
Not indented:
 By one space: |
    By four
      spaces
 Flow style: [    # Leading spaces
   By two,        # in flow style
  Also by two,    # are neither
  	Still by two   # content nor
    ]             # indentation.

```


# Error
```text
reformat: formatter output had syntax errors where input had none:
spec-example-6-1-indentation-spaces.yml:10:3 parse ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  × Insufficient indentation in flow collection
  
     8 │   Flow style: [    # Leading spaces
     9 │    By two,        # in flow style
  > 10 │   Also by two,    # are neither
       │   
    11 │   	Still by two   # content nor
    12 │     ] # indentation.
  

```
