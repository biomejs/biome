---
source: crates/biome_formatter_test/src/snapshot_builder.rs
info: yaml/spec/spec-example-5-5-comment-indicator.yml
---

# Input

```yml
# Comment only.

```


# Error
```text
reformat: output mismatch
--- re-formatted
+++ formatted
@@ -1,4 +1,2 @@
 # Comment only.
 # Comment only.
-# Comment only.
-# Comment only.

IR differences:
--- re-formatted IR
+++ formatted IR
@@ -1,13 +1,5 @@
 [
   "# Comment only.",
   hard_line_break,
-  "# Comment only.",
-  hard_line_break,
-  verbatim([
-    "# Comment only.",
-    hard_line_break,
-    "# Comment only.",
-    hard_line_break,
-    ""
-  ])
+  verbatim(["# Comment only.", hard_line_break, ""])
 ]
\ No newline at end of file

```
