---
source: crates/biome_formatter_test/src/snapshot_builder.rs
info: yaml/spec/aliases-in-explicit-block-mapping.yml
---
# Input

```yml
? &a a
: &b b
: *a

```


# Prettier differences

```diff
--- Prettier
+++ Biome
@@ -1,2 +1,3 @@
-&a a: &b b
-: *a
+? &a a
+: &b b
+: *a
\ No newline at end of file
```

# Output

```yml
? &a a
: &b b
: *a```

# Errors
```
aliases-in-explicit-block-mapping.yml:1:3 parse ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  × Unexpected character `&`
  
  > 1 │ ? &a a
      │   ^
    2 │ : &b b
    3 │ : *a
  
aliases-in-explicit-block-mapping.yml:2:3 parse ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  × Unexpected character `&`
  
    1 │ ? &a a
  > 2 │ : &b b
      │   ^
    3 │ : *a
    4 │ 
  
aliases-in-explicit-block-mapping.yml:3:3 parse ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  × Unexpected character `*`
  
    1 │ ? &a a
    2 │ : &b b
  > 3 │ : *a
      │   ^
    4 │ 
  

```
