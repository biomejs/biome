---
source: crates/biome_formatter_test/src/snapshot_builder.rs
info: yaml/plain/middle-comment.yml
---

# Input

```yml
!!str # comment
hello

```


# Error
```text
reformat: output mismatch
--- re-formatted
+++ formatted
@@ -1,2 +1,2 @@
 !!str hello
-# comment # comment # comment # comment
+# comment # comment

IR differences:
--- re-formatted IR
+++ formatted IR
@@ -1,8 +1,8 @@
 [
-  verbatim(["!!str"]),
+  verbatim(["!!str", line_suffix([" # comment"]), expand_parent]),
+  line_suffix([" # comment"]),
+  expand_parent,
   " hello
-# comment # comment",
-  line_suffix([" # comment # comment"]),
-  expand_parent,
+",
   hard_line_break
 ]
\ No newline at end of file

```
