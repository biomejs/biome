parent:
  one: 1
  # sibling of one
valueless:
  # in the value slot
empty: # on the key line
  # below it

  # blank kept above
seq:
  - deep: 1
    # sibling of deep
  # sibling of the item
last: 1
